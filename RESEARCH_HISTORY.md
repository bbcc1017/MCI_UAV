# MCI-UAV 연구 발전사 — 저장소 최초 커밋부터 v21 까지

> 작성 2026-09-06, 현행 상태·과거 기각의 정정 2026-09-09. 대상 저장소 `bbcc1017/MCI_UAV` (**최초 커밋 2026-05-06**, v21까지).
> **상세판**: 이 문서는 요약이고, 시대별 전문(사료 목록·CSV 교차검증 대조표 포함, 총 340KB)은
> `docs/연구발전사_상세/hist_era{1,2,3,4}.md` 에 있다 —
> era1 태동~v5 · era2 v6~v12 · era3 v13~v17 · era4 v18~v19.
> 논문 자산 정합 감사·문헌 위치·통계 감사는 `docs/v20_감사/{paper_fit,lit,stats}.md`.
> 현재 판정 계약: [agent_docs/research.md](agent_docs/research.md). 과거 지침 전문은 `git show 0b749f07e:AGENTS.md`.
> 시대별 근거: `RESEARCH_LOG.md`(v3~v5 상세) ·
> `docs/README.md`(v1~v4 연대기) · `archive/docs/reports/`(보고서 29편) ·
> `docs/v17_*.md` · `docs/v18_*.md` · `archive/docs/presentations/260903랩미팅/`(v19) · `results/scoreboard/v10~v21/`.
> 이 문서는 **무엇을 했나** 가 아니라 **무엇이 틀렸다고 판명나서 방향이 어떻게 꺾였나** 를 기록한다.

---

## 0. 한 장 요약 — 연구질문은 세 번 바뀌었다

이 연구는 "한국에 UAV(무인 이송수단)를 도입하면 대량사상자 사고 대응이 얼마나 좋아지는가" 를
강화학습으로 답하려고 시작했다. 그런데 질문은 세 번 갈아탔다.

1. **성능 국면 (v1 ~ v6)** — "RL 이 기존 휴리스틱을 이기는가."
   이겼다. 그런데 그 과정에서 **RL 로그를 분석해 만든 한 줄짜리 규칙(발송상한 LB-T)이 RL 본체를
   이겨버리는 사건**이 일어났다. 이것이 연구의 성격을 바꿨다. 이후 전부는 "왜 규칙이 이겼나" 의 후속이다.
2. **해석 국면 (v7 ~ v15)** — "RL 이 무엇을 알고 있는가, 그것을 꺼낼 수 있는가."
   반응형 정책의 천장(오라클 대비 57%)을 확인하고, 그 천장을 룩어헤드로 뚫고(NCRP·MILP),
   그 결과를 트리·규칙으로 증류했다. 그 과정에서 **룩어헤드는 반응형 정책이 흡수할 수 없다**는 것을
   다섯 번 서로 다른 방식으로 확인했고, **행동 모방 정확도(fidelity)는 성능의 대리변수가 아니다**는 것을
   다섯 번 확인했다.
3. **방법론 국면 (v16 ~ 현재)** — "RL 에서 현장 규칙을 뽑는 절차 자체를 정립할 수 있는가."
   물리단위 현장 규칙집 CARD 를 만들고, 그 임계값이 어디서 오는지 캐고(v18 `mine`),
   관측 설계를 고쳐 교사를 다시 세우고(v19), **임계값의 물리적 스케일링과 전이성**을 검증했다(v20).
   v21에서는 30시드·최강 교사와의 판정을 정합화하고 통신 요구가 다른 현장 카드를 비교했다.

한 문장으로: **성능 경쟁에서 시작해, 규칙이 RL 을 이기는 사건을 겪고, 결국 "RL 을 규칙 설계
도구로 쓰는 절차" 를 만드는 연구가 됐다.**

---

## 1. 연대기

### 1막. v1 — 첫 전국 RL, 그리고 자기 규칙에게 역전당함 (2026-05-06 ~ 06)

| 항목 | 내용 |
|---|---|
| 질문 | 전국 어디서 사고가 나도 통하는 단일 RL 이송정책을 만들 수 있는가 |
| 실험 | 이벤트 기반 MCI 시뮬(`src/sim_src/`) 위에 gym 환경 → 지역별 병렬학습(피드백1) → 전국 단일 `MultiRegionEnv`(피드백2) → obs 축소(피드백3) → 다알고리즘 |
| 수치 | 전국 단일 RL 이 통용 64룰 휴리스틱을 이김. 단 격차 +1~2 수준으로 "압도" 는 아님 |
| 판정 | 성능 목표는 형식상 달성, 실질은 미달 |
| 방향전환 | RL **로그를 분석**하기 시작 → 여기서 사건이 터진다 |

**사건**: 로그를 보니 휴리스틱의 목적지 선택("발송게이트를 통과한 최근접 병원")이 최근접 1~2곳에
환자를 몰아 점유율 ~500%, gini 0.94 로 과집중시키고 있었다. 목적지만 "적격 병원 중 **누적발송
`p_sent < T`(≈4)** 인 최근접, 차면 다음" 으로 한 줄 바꾼 **LB-T4** 가
시도17 +2.77/+3.11(17전 17승) · 시군구250 +2.44/+1.99 로 **RL 본체를 240·245/250 지역에서 능가**했다
(`src/rl_src/loadbalance_heuristic.py`, `LOADBALANCE_HEURISTIC_REPORT.md`).

부수 발견 둘. ① 현행 시나리오는 병원 **발송** 용량이 부하의 6~16배인 저스트레스 영역이라
어떤 정책이든 격차가 눌린다. ② 병원 실시간 통신을 끊어도 성능차가 사실상 없다(18지역 ×2,
평균 +0.008, 잡음 ~0.39 안쪽 — `RL_occ_siteonly_report_2026-06-24.md`). 통신축은 null 이었다.

### 2막. 진단과 재설계 v2 — 왜 RL 이 자기 규칙에게 졌나 (2026-07-02 ~ 07-05)

세 결함을 특정했다(`연구방향_재점검_2026-07-02.md`).
① **관측 결손** — 이기는 규칙이 쓰는 신호(병원별 누적발송, 이송 중 환자, 점유율)가 obs 에 없었다.
② **스케일 앵커 부재** — ETA 가 좌표별 정규화되어 있어 전국 단일 정책이 절대 시간을 표현하지 못했다.
③ **평탄 head** — 목적지 랭킹 구조가 없었다.

같은 시기 **성남시의료원 헬기장 정정(0→1)** 으로 병원집합이 46→47, 헬기장 25→26 으로 바뀌어
전 1,534 좌표를 재구축했고(2026-07-02), Green 을 action 차원에서 제거해 action 282→192 가 됐다(07-03).
**이 시점 전후 수치는 혼용 금지**다(구 산출물은 `archive/results_20260702_헬기장정정이전/`).

재설계 v2 는 obs 를 `essential+load`(dim 355)로 바꾸고 목적지 랭킹 head(`pointer_policy.py`)를 넣었다.
결과: **L3(부하 obs + 포인터)가 LB-T4 를 역전**(시도17 −27.7%, holdout250 85% 승).
RL 이 규칙을 다시 앞섰다.

### 3막. 상한을 재다 — v3·v4·v5 (2026-07-06 ~ 07-14)

| 버전 | 질문 | 결과 | 판정 |
|---|---|---|---|
| v3 | 반응형 정책의 상한은 어디인가 | 미래를 훔쳐보는 오라클 대비 챔피언은 **56%** 지점. ExIt(오라클 라벨 → BC → 파인튠) 실패 | 잔여 44% 는 룩어헤드 전용이라는 **1차 증거** |
| v3 | 규칙으로 얼마나 회수되나 | 12항 선형 점수식이 격차의 **65.5%** 회수 | 채택. 나머지는 "전국 단일 규칙" 형태의 구조적 한계 |
| v4 | 구조를 키우면 넘는가 | obs `+ctx`(dim 502) · attention ≥2블록 · λ 변형 **4종 전원 기각**. 채택은 반복 재가중뿐 | **반응형 포화의 2차 증거** |
| v4 | — | 오라클 도달률 56 → **57.1%** | 수확 체감 확인 |
| v5 | PPO 말고 다른 알고리즘은 | DQN 0.190 / QRDQN 0.245 / SAC-D 0.274 / REINFORCE 0.215 — 64룰(0.234)은 넘어도 **LB-T4(0.120)에는 전부 0/17 완패** | "PPO만 규칙을 압도" 실증 |
| v5 | 천장을 계산으로 뚫을 수 있나 | **NCRP**(비천리안 제한 롤아웃 플래너) 채택 → 도달률 57.1 → **65.4%** | 학습이 아니라 **결정시점 계산**이 천장을 뚫었다 |

v4 에서 얻은 규칙 하나가 이후 전 실험을 지배한다: **훈련곡선 중간 우세는 채택 근거가 못 된다**
(`v4_full` 은 곡선 1위였고 판정은 꼴찌였다).

### 4막. 차원 비의존과 표현력의 벽 — v6 ~ v12 (2026-07-20 ~ 07-30)

- **v6**: 병원 수를 고정 47 로 전제하던 것을 깨고 `MCI_H_PAD` 패딩 + `valid` 열 + 마스크드 풀링으로
  **병원 수 가변을 성능손실 0 으로 소화**했다(고정47 챔피언 동률, 자연-H 는 zero-shot 추월).
  ExIt 은 여기서도 실패(비천리안 라벨 switched acc 0.125) — **룩어헤드 흡수불가 3중 확증**.
- **v7**: 행동 모방이 안 되면 **가치 쪽으로** 흡수해보자(value-guided PPO). 가치 게이트는 통과했는데
  (q_best R² 0.886) 폐루프는 **개입 강도에 비례해 단조 악화**. **5중 확증**으로 종결.
  이후 룩어헤드는 "배포 시 플래너 실행" 으로만 쓴다.
- **v8·v9**: Pointer head 를 3원(class×dest×mode)으로 확장하거나 잔차항을 붙이는 시도 — 전부 기각.
  대신 **이득이 난이도 상위 좌표에 몰린다**는 지역효과를 발견.
- **v10**: 데이터 분할을 정본화했다(학습 = random4 1,000 / 평가 = 대표점 250). 그리고 **증류 트랙**을 열어
  후보랭킹 CART·GBDT 로 교사를 근사했다(I1 GBDT 가 외부 250 좌표에서 PPO 를 추월).
- **v11**: NCRP 조건을 h10m16 → **h20m16** 으로 교체(+0.00176±0.00036 유의). **MILP** OR 기준선을 새로 만들었다.
  여기서 검정 관례 두 개가 확립됐다 — **팔 대 팔 paired 필수**(base 대비 Δ만 보면 결론이 뒤집힌다),
  **부분집계 편향**(에피소드 비용 ∝ 난이도라 완료순서가 난이도와 역상관 → 진행 중 CSV 로 판정 금지).
- **v12**: 교수 제안(transformer)을 GOPT 이식으로 검증 — **전부 기각**(표현력 확장 네 번째 음성).
  대신 **attention 제거**가 채택됐다(3시드 +0.00229·3.48σ, 시드 sd 가 1/28). 그리고 이 프로젝트에서
  가장 자주 쓰이게 되는 상수가 여기서 처음 측정됐다 — **학습 시드 잡음바닥 sd = 0.00114**.
  또한 LB-T 전수 스윕으로 전국 최적 T 를 4 → **3** 으로 정정했다.

### 5막. 증류에서 규칙으로 — v13 ~ v17 (2026-08-03 ~ 08-20)

- **v13**: 최종교사(PPO+NCRP+MILP, 0.140286)의 실행행동을 19개 학생에게 증류.
  최상위 GBDT 가 **교사와 통계적 동률**(Holm p=0.6139)인데 **exact fidelity 는 31.0%**(dest 32.4%).
  → **행동일치도는 성능의 대리변수가 아니다.**
- **v14**: 규칙화. `GUIDE_FINAL_LBT3` 가 격차의 76.7% 회수. 단 성분 ablation 이 주장을 좁혔다 —
  본체는 class 축(+0.025)이고 **UAV 임계 기여는 +0.00034 로 시드 잡음 이하**였다.
- **v15**: 구조 역전(증류 학생을 base, RL·MILP 를 후보 제안자, NCRP 를 검증자)까지 갔고 동결·사전등록까지
  했으나 **블라인드 250 에서 음성**(FINAL 이 BASE_G1 보다 0.002036 악화). 포트폴리오는 보조분석으로 내렸다.
- **2026-08-06~08 랩미팅**: 메인트랙을 확정했다 — **PPO 를 교사로, 재정의한 START-LB3 를 기준선으로,
  결정로그 분석에서 현장 규칙집을 뽑고 독립 좌표 폐루프로 검증한다.** 동시에 LB-T3 코드 감사로
  "T=3 은 병원당 3번째 누적 발송까지 허용하는 soft balancing threshold" 라는 정확한 의미를 확정했다.
- **v16**: 비교군을 분해해 **1억 에피소드** 전수평가. 결론이 랩미팅의 우려를 정량으로 답했다 —
  **사후선택 없는 단일 규칙 `LB3-AGN-RR-FAST`(0.160887)가 좌표별 Best-of-64 를 고른 뒤 T3 를 씌운
  `HEURBEST→CAP`(0.165837)보다 좋다.** 사후선택은 이득이 아니라 손해였다.
- **v17**: 두 갈래.
  ① **특징증강 증류**(Yan et al. 2026 대응) — 재현율 0.3268 → 0.3309, 폐루프 격차 회수 47.6 → 60.8%.
     동시에 **모델 클래스가 특징보다 16배 큰 레버**이고 **중요도 질량 51.7% 를 먹고도 정보이득은 +0.004**임을 확인.
  ② **★현장 규칙집 CARD** — 정규화 ETA 대신 **물리단위(km·명)** 로 3단 규칙을 세웠다.
     대표점250 0.14149 vs PPO 교사 0.14844 로 **규칙이 교사를 추월**했고 외부 250 좌표에서도 재현됐다.
     그리고 그 원인이 구조가 아니라 **정보**임을 진단했다 — 거리를 PPO 관측 형태(좌표별 정규화)로
     제약하면 0.16449 로 무너지고 손실의 92% 가 정규화에서 나온다.

### 6막. 방법론화와 관측 재설계 — v18 ~ v19 (2026-09-01 ~ 09-03)

- **v18**: 성공 사례 하나를 **재현 가능한 절차**로 승격시키는 트랙.
  - 발표 카드 1단계가 **실제로 평가된 정책이 아니었다**는 것을 잡아냈다(실행은 `yhold=0`, 카드는 "14명").
  - 정직한 교사 기준선을 다시 세웠다 — v17 이 쓴 v10_s0 는 3시드 중 최악이었고, 최강 교사 대비
    CARD 의 마진은 +0.0069 가 아니라 **+0.0038~0.0042** 였다.
  - 임계값 도출 절차를 `v17_field_rules.py mine` 으로 **코드화**하고 22개 항목을 재현했다.
  - **변수선택 기여는 총 격차의 5.5% 뿐**이고 나머지 ~95% 는 **함수형 교체**(이진 발송상한 → 연속 교환율)였다.
  - **지역화 상한(L3 오라클)은 격차의 12.4%** 이고 전국 단일 규칙 하나가 이미 87.6% 를 먹는다.
- **v19**: 관측을 **현장 지휘소가 실제로 아는 정보만**으로 다시 설계했다(dim 402 → 389, 정보를 뺐다).
  누출 3종(차량 실시간 잔여시간 · 병원 실시간 · 미구조 환자 등급)을 제거했다.
  - **v17 의 결론이 뒤집혔다** — test750 에서 `PPO_SIDO` 0.138839 · `PPO_NATIONAL` 0.139738 <
    최강 규칙 `CARD_GRID_S` 0.144410. **RL 이 규칙을 다시 앞선다.**
  - 진짜 원인은 raw 스케일이 아니라 **슬롯별 VecNormalize** 였다(거리순 정렬 슬롯 × 순열등변 head →
    같은 20분이 슬롯 0 에서 +0.21σ, 슬롯 46 에서 −6.07σ).
  - 판정축을 대표점250 → **test750** 으로 옮겼다(대표점250 은 OSRM 스냅 오염 12.4%>500m).

### 7막. 임계값 일반화 — v20 (2026-09-06, 결과는 §4-3)

질문: **CARD 의 임계값은 특정 시나리오(환자 100 · AMB 30 · UAV 26 · 50/200 km/h · 인계 5/10분)에
과적합된 상수인가, 아니면 시스템 물리로 예측되는 구조상수인가.**

방법 두 갈래로 검증했다.
① **규칙 응답면 스윕** — 런타임 물리 노브(신규 `MCI_AMB_VELOCITY`/`MCI_UAV_VELOCITY`/
   `MCI_AMB_HANDOVER`/`MCI_UAV_HANDOVER` + 기존 `MCI_AMB_NUM`/`MCI_UAV_NUM`/`MCI_INCIDENT_SIZE`/
   `MCI_CAPA_SCALE`)로 축을 하나씩 바꾸며 임계값 격자를 폐루프 전수 실행한다. RL 재학습이 필요 없다.
② **교사 재학습 6런** — `results/rl/v20/{amb10,amb20,uav6,n200,n50,capa05}`, v19 레시피 동일·물리만 변주.
   자원축 곡선에서 OOD 교란을 제거하고, 교사가 스스로 채굴하는 임계값의 이동을 본다.

첫 결과(base 설정, 250좌표 × 10시드, 28팔 CRN)가 이미 이론을 지지한다 — §4-3.

### 8막. 판정 정합성 감사와 정보수준 카드 — v21 (2026-09-08)

랩미팅 직전 질문 하나가 트랙을 하나 더 열었다 — **"저번주는 750좌표 × 30시드였는데
이번주 자료는 10시드다. 시드를 줄여서 규칙이 교사를 앞선 결론이 나온 것 아닌가."**

감사 결과는 셋으로 갈렸다.

1. **시드 축소는 결론을 만들지 않았다.** v20 시드 0–9 산출이 v19 부분집합과 비트동일
   (maxΔ=0.000e+00)이라 30시드를 복원할 수 있었고, 되돌리자 마진이 **커졌다**
   (+0.001076 → +0.001293). 시드 0–9 는 규칙에 **불리한** 표본이었다.
2. **정밀도 지적은 정당했다.** Δ 의 시드축 95%CI 가 10시드 ±0.00062 → 30시드 ±0.00036.
   특히 최강 교사 대비는 10시드에서 CI 가 0 을 가로질러 아무 말도 할 수 없었다.
3. **★결론을 실제로 바꾼 것은 교사 기준선이었다.** v20 은 교사를 전국 단일 하나로 놓았지만
   v19 최강 교사는 광역시도 17벌(0.138839)이다. 넣으면 유도형 규칙(0.138445)의 마진은
   +0.000393±0.000340 = **판정선 미만 동률**. **v18 에서 이미 교정한 실수의 재발**이다.

같은 세션에서 "현장 정보만" 피드백을 축으로 **(정보수준 I0~I3) × (선형·초과분·초과분÷수술실수)**
격자를 닫았다(부하항 3종 신설, budget750 52팔 39만 에피소드). 여기서 이 프로젝트에서 가장
반직관적인 결과가 나왔다 — **통신을 끊는 비용은 +0.00076 인데, 병원 재고만 묻고 내 발송을
세지 않으면 +0.0929 로 붕괴한다.** 기전은 **정보 지연**이었다(병원 census 는 도착·접수 후에야
올라가므로 dispatch burst 의 연속 결정이 같은 빈 병원을 반복 지정 → 쓴 병원 수 5.8곳,
부하 무시 4.96곳과 사실상 같다). 결정에 필요한 정보는 병원 전산이 아니라 현장 장부에 있었다.

---

## 2. 기각 목록 (재시도 전 반드시 읽을 것)

각 결과는 당시 분할·예산·정보수준에 조건부다. 후속 실험에서 뒤집힌 판단은 정정을 함께 읽는다.
이 표를 모든 설정에서의 불가능성이나 새 근거가 있어도 재시도할 수 없다는 금지로 해석하지 않는다.

| # | 기각한 것 | 언제 | 왜 |
|---|---|---|---|
| 1 | obs `essential+load+ctx`(dim 502) | v4 | 시도17 전부 열세. 반응형 관측 포화 |
| 2 | attention 블록 ≥2 | v4 | 동상. 이후 v12 에서 **0 블록이 채택**됨 |
| 3 | SMDP γ^Δt | v5 | DQN 0.190 → 0.243 악화 |
| 4 | 리프 가치망 | v5 | val MAE 0.065 가 후보 간 미세 Q차이를 압도 |
| 5 | 결정귀속 보상 `pdrwog_da` | v5 | GAE 가 이미 신용할당 처리. ETA 근사가 편향 주입 |
| 6 | ExIt 오라클 증류 | v3·v6 | switched 한정 held-out acc 0.125~0.19 |
| 7 | value-guided PPO / CRR | v7 | 개입 강도에 비례해 단조 악화. **룩어헤드 흡수불가 5중 확증** |
| 8 | Pointer 3원 head(v8) · 잔차 head(v9) | v8·v9 | 표본공유 상실 / 승자 없음 |
| 9 | GOPT bilinear·크로스어텐션·풀GOPT | v12 | 전부 열세. 용량 동수 대조군에서도 열세 = 구조 이득 아님 |
| 10 | pooled critic 단독 | v12 | 1.03σ, 시드 운 |
| 11 | 선택적 위임(score-margin) | v10 E | dev40 개선이 외부에서 재현 안 됨 |
| 12 | v15 포트폴리오 추가보정 | v15 | 블라인드 250 에서 사전등록 주비교 음성 |
| 13 | 지형층 공식 지역화(L1) | v18 | 시도 홀드아웃에서 소멸(이득의 73%가 학습좌표 효과) |
| 14 | `p_sent` 부하신호에 대한 **당시 기각 → 철회** | v17 → v18~v21 | 행동회귀의 음수 계수로 기각했으나 폐루프에서 재평가. v21 P18은 현장 장부 기반 권장 카드; 행동 적합으로 변수 유용성을 판단한 오류 |
| 15 | 8잎(사람 크기) 증류 트리 | v17 | 0.2605 로 HEUR64 사후선택에도 미달 |
| 16 | obs 클립·포화형·생존함수 재인코딩 | v19 | 전부 증상치료(진짜 원인은 슬롯별 VecNormalize) |
| 17 | 가치기반·PG 계열(DQN/QRDQN/SAC-D/REINFORCE) | v5 | 64룰은 넘어도 LB-T4 에 0/17. baseline 표 외 용도 금지 |
| 18 | 병원 재고(occ) 단독을 부하신호로 | v21 | 0.2319 로 부하 무시(0.2594)에 근접 붕괴. 정보 지연 탓 — 내 발송(p_sent)을 반드시 함께 센다 |
| 19 | 선형 부하항까지 단순화한 현장 카드 | v21 | 0.1470 으로 현행 규칙집(0.1450)보다 나쁘다. 권장선은 초과분 형태까지 |

---

## 3. 방법론적 교훈 — 이 프로젝트가 실험으로 얻은 실험 설계 규칙

1. **훈련곡선 중간 우세는 채택 근거가 못 된다** (v4: 곡선 1위 → 판정 꼴찌).
2. **판정은 팔 대 팔 paired 로** 한다. base 대비 Δ만 보면 결론이 뒤집힌다(v11 이 v6 의 "h는 죽은 축"을 반증).
3. **진행 중 CSV 로 판정하지 마라.** 에피소드 비용 ∝ 난이도라 완료순서가 난이도와 역상관이다(v11).
4. **학습 시드 잡음바닥 = sd 0.00114.** 아키텍처 판정은 3시드 평균 대 이 바닥, 배수 ≥2 (v12).
   에피소드 CI 는 시드 잡음을 담지 않으므로 그것만으로 아키텍처를 판정하면 안 된다.
5. **승/무/패는 지역별 에피소드 배열의 95%CI 유의성**이지 지역평균 임계값이 아니다(v5).
6. **모방 최적 ≠ 성능 최적** — 최소 5회 재현. 규칙 파라미터는 재현율이 아니라 폐루프 PDR 로 고른다.
7. **행동일치도(fidelity)는 성능의 대리변수가 아니다** (v13: fidelity 31% 인데 교사와 폐루프 동률).
8. **중요도 ≠ 정보이득** (v17: 새 특징이 중요도 51.7% 를 먹고 재현율은 +0.004).
9. **사후선택 기준선은 배포규칙이 아니다.** 그리고 사후선택이 오히려 손해일 수 있다(v16).
10. **기본값 = 구 동작 비트동일.** 새 기능은 opt-in 이고 회귀 게이트로 증명한다(전 버전 공통).
11. **부분상관을 보라.** 원거리성을 통제하지 않으면 인프라 특징이 전부 "중요" 해 보인다(v19).
12. **정보 제약을 맞추고 비교하라.** 규칙이 이긴 원인이 구조인지 정보인지는 정보를 맞춰야 갈린다(v17).
13. **판정셋 시드 수는 결과를 보기 전에 고정한다.** 줄일 때 어느 팔에 유리한지 사전에 알 수 없다(v21).
14. **판정표의 "교사" 는 계열이 아니라 그 계열의 최강값이다** (v18 → v21 재발). 학습정책이
    표에 등장하면 학습 시드 잡음(sd 0.00114)을 함께 적고, 그보다 작은 마진으로 우열을 말하지 않는다.
15. **규칙 대 학습정책 비교는 시드축 CI 를 따로 보고한다.** CRN 이 지역 잡음은 잡지만 시드
    잡음은 그만큼 남는다 — 시드별 Δ 의 sd 가 0.0010 규모다(v21).
16. **정보는 양이 아니라 갱신 시점이다.** "실시간" 이라는 말이 "즉시" 를 뜻하지 않는다(v21).

---

## 4. 수치 정본표와 비교 금지선

### 4-1. 비교 금지선 (이걸 틀리면 표 전체가 무너진다)

| # | 경계 | 이유 |
|---|---|---|
| A | **2026-07-02 성남 헬기장 정정 전/후** | 병원 46→47, 헬기장 25→26, obs·action 차원 변경. 구 산출물은 `archive/results_20260702_헬기장정정이전/` |
| B | **sim 정정 커밋 `b01efd3`(2026-08-12 15:04) 전/후** | 병원 후보 순회를 인덱스순 → 수단별 실제 ETA 순으로 정정. v16 전량이 이전 산출물이다 |
| C | **대표점250 vs sigungu30 test750** | 대표점250 은 OSRM 스냅 오염(12.4% > 500m, 최대 1,901m). test750 은 생성단계 ≤500m 게이트 |
| D | **사후선택 oracle 형 vs 단일 배포규칙** | `HEUR64 Best-of-64`·`LB3-CAPBEST64`·`STARTBEST32` 는 평가 후 발췌한 분석값이지 배포규칙이 아니다 |
| E | **에피소드 수 규약** | v10 정본은 1000ep(seed 0..999). 30ep 는 NCRP 비용 때문에 자른 부분집합이고 지역별 argmin 에 +0.002085 선택편향을 섞는다 |

### 4-2. 판정셋별 대표 수치 (PDR_woG, 낮을수록 좋음)

**대표점250 (seed 0–29, v17 하네스 — 경계 C 이전 축)**

| 팔 | PDR_woG |
|---|---:|
| HEUR64 Best-of-64 (사후선택) | 0.2384 |
| START-LB3 | 0.164056 |
| 증류 트리 GBDT31 (v17 특징증강) | 0.154554 |
| PPO 교사 v10_s0 | 0.148390 |
| PPO 교사 ATTN0 3시드 평균 (정직 기준) | 0.145298 |
| **CARD 현장 규칙집** | **0.141493** |
| PPO + NCRP h20m16 | 0.141475 |
| 최종스택 PPO+NCRP+MILP | 0.140286 |

**test750 (seed 0–29, 정책당 22,500ep — 현행 판정축, v21 에서 전 팔 30시드 통일)**

| 팔 | PDR_woG | 최강 교사(PPO_SIDO) 대비 |
|---|---:|---|
| `Z0` 부하 무시 | 0.259736 | — |
| `OR32`·`OH27`·`OL18` 병원 재고만 | 0.2325 / 0.2320 / 0.2248 | 붕괴 |
| `START_LB3` | 0.167356 | −0.02852 |
| `FULL64_LB3` | 0.164781 | −0.02594 |
| `PL9` 통신 X · 선형 | 0.147008 | −0.00817 |
| `T9` 통신 O · 선형 | 0.146320 | −0.00748 |
| `CARD_K12` 현행 현장 규칙집 | 0.144985 | −0.00615 |
| `CARD_GRID_S` v19 최강 규칙(격자 시도별 17벌) | 0.144410 | −0.00557 |
| `PH9` 통신 X · 초과분 (카드 C) | 0.142041 | −0.00320 |
| `CARD_H12` | 0.140783 | −0.00194 |
| `PPO_NATIONAL` field obs 전국단일 | 0.139738 | −0.00090 |
| `CARD_P18` 통신 X · 유도형 (카드 B) | **0.139207** | −0.00037 **동률** |
| `CARD_S062` 무튜닝 (카드 A′) | 0.139009 | −0.00017 **동률** |
| `PPO_SIDO` field obs 광역시도 17 — **최강 교사** | **0.138839** | (기준) |
| **`CARD_Q18` 유도형 (카드 A, 채택)** | **0.138445** | **+0.00039 ± 0.00034 동률** |

⚠️ **교사와의 우열은 주장하지 않는다.** 최강 교사 대비 마진이 판정선(CRN paired 0.00053)
아래이고 교사 학습 시드는 계열당 1개(잡음 sd 0.00114)다. 견고한 주장은 **현행 규칙집 대비
+0.00654(626승 122무 2패)** 와 **START-LB3 대비 +0.02891(728/17/5)** 이다.
v20 문서의 10시드 절대값(Q18 0.138692 등)은 이 표로 대체된다.

**전수평가 (v16, 1,250좌표 × 1000ep — 경계 B 이전)**

| 팔 | 대표점250 | 학습1000 |
|---|---:|---:|
| `LB3-HEURBEST→CAP` (사후선택) | 0.165837 | 0.173039 |
| `LB3-AGN-RR-FAST` (단일 규칙) | 0.160887 | 0.167462 |
| `SHIN-ALIGN-HOSP` Best-of-16 | 0.245272 | 0.253943 |

### 4-3. v20 결과 — 임계값은 튜닝 상수가 아니라 구조상수였다

물리 축 7개 × **28 설정** × 규칙 형태 6 가족 전수. 설정마다 같은 가족 팔을 한 실행에 넣어
같은 좌표·같은 시드(CRN). 총 **392만 에피소드** + 정본 튜닝셋 재도출 20만 + 교사 재학습 6런(10M).

**목적지 규칙의 형태 사다리** (등급·수단 단계 동일, 목적지 점수만 교체)

| 가족 | 목적지 점수 | 기준 최적 | PDR_woG | 현행 CARD 대비 |
|---|---|---:|---:|---|
| **K** | `거리(km) + λ·부하(명)` — 현행 CARD | 12 km/명 | 0.145769 | (기준) |
| T | `도달시간(분) + λ·부하(명)` | 9 분/명 | 0.147328 | **−0.00156 열세** |
| L | `거리(km) + λ·max(0,부하+1−c)` | 14 km/명 | 0.141422 | +0.00435 |
| **H** | `도달시간(분) + λ·max(0,부하+1−c)` | **12 분/명** | 0.141439 | +0.00433 ± 0.00063 |
| **Q** | `도달시간(분) + λ·max(0,부하+1−c)/c` | **18 분/명** | **0.138980** | **+0.00679 ± 0.00066** |
| S | 생존확률 직접 최대화 | w 0.62 | 0.140046 | +0.00573 |

c = 그 병원의 **수술실수**(치료개시 게이트, 중앙값 2). ⚠️기존 문서의 "용량이 부하의 6~15배" 는
**발송 게이트**(수술실수+병상수, 중앙값 14) 이야기이고 치료개시를 막는 것은 수술실수다.

- 배포형 고정상수 비교(선택편향 0): **H12 vs K12 = +0.00499, 28/28 유의 우세**,
  Holm 보정 최악 **p = 4.57e-12**, 좌표 부트스트랩 4,000회에서 28설정 전부 유지(비율 1.000).
- **단위만 바꾼 T 는 16/28 유의 열세** → **성능은 함수형이, 전이성은 단위가 만든다**(분리된 두 효과).
- 가족별 최선: **Q 가 28 설정 중 27**(나머지 1은 S).

**전이 후회** — 기준 조건에서 고른 상수를 27개 다른 조건에 그대로

| 가족 | 기준 상수 | 중위 | 평균 | 최대 | paired 유의 열세 |
|---|---|---:|---:|---:|---:|
| **Q** | 18 분/명 | +0.00002 | **+0.00010** | **+0.00057** | **1/27** |
| H | 12 분/명 | +0.00000 | +0.00035 | +0.00211 | 6/27 |
| K | 12 km/명 | +0.00021 | +0.00108 | +0.00964 | 9/27 |
| T | 9 분/명 | +0.00005 | +0.00109 | +0.00686 | 9/27 |

⚠️ **2026-09-11 정정 — 이 27조건에 치료시간(서비스시간) 축이 없다.** `results/scoreboard/v20/transfer.csv`
와 `optima.json` 실측 확인 결과 `ts`/`treat` 조건이 0개다. 즉 "λ = 서비스시간" 주장과 "λ는 전이된다"
주장이 **서로소 조건집합**에서 계산됐다. λ가 비례하는 축을 넣으면 Q족 최대 후회가 +0.00057 →
**+0.02775**(치료시간 ×4, budget750 750좌표)로 커진다. 다른 축은 넣어도 +0.0014 이하다.
정정된 정본은 `agent_docs/research.md` §v22.

**스케일링 — 구조 보정마다 용량 의존이 정확히 한 단위씩 사라진다.** 보간 최적의 로그-로그 기울기
(좌표 클러스터 부트스트랩 95%CI): 용량축 **K −1.297 [−1.333, −1.241] → H −1.032 [−1.073, −0.821]
→ Q −0.198 [−0.250, −0.141]**. H 의 CI 는 이론값 −1 을 포함하고 0 을 배제하며, **K 의 CI 는 −1 을
배제**한다(거리축에서는 −1 이 나오지 않는다). Q 의 남은 기울기는 7축 전부 |·| ≤ 0.20.

**잔여 의존성의 기전** — H(단일 λ)에서 λ\*가 12→8 로 내려가는 조건은 전부 **UAV 이송 비율 ≥ 0.49**
이고, 헬기장 병원의 수술실수가 더 많다(2.462 vs 1.782, 비 1.382 → 예측 λ_uav = 8.69).
수단별 λ 로 갈면(λ_amb 12 고정) **λ_uav\* = 6 이 자원·속도 축 8/10 설정에서 동일**해 잔여가 닫힌다.
Q 형태는 병원별 c 로 나누므로 **수단 분리 자체가 불필요**하다.

**★기전 계측 — 정확한 손실 분해로 확정했다.** 보상 정의상 `PDR = 미진입손실 + 지연손실` 이 항등식으로
성립하므로(계측기 `v20_mechanism_eval.py`, 실측 잔차 6.7e-16) 기전을 추측 없이 쟀다.
7개 물리조건 × 규칙 9종 × 250좌표 × 10시드.

- **미진입손실은 전 조건에서 정확히 0** — 구조된 Red/Yellow 는 전부 결국 수술실에 들어간다.
  따라서 PDR 은 **100% "언제 들어갔나"** 의 문제이고 "진입률" 은 변별력이 없다(지표에서 제외).
- **부하항은 대기시간을 줄인다** — 부하를 안 보면 병원 5곳에 몰려 **66.5% 가 병상 대기, 중위 51분**.
  최적값에서 11.5%·20.9분이고 **Yellow 치료개시가 82분 → 53분으로 29분 앞당겨진다.**
- **λ 의 교환이 지표로 직접 보인다** — λ 과대(50)면 대기는 3.4%·18.7분으로 최소지만 더 멀리 가야 해서
  치료개시가 늦어진다(53.1 → 54.3분). λ=18 에서 치료개시 시각이 최소이고 PDR 도 최소다.
  ⚠️대기 지표 하나만 보면 오판한다.
- **대기가 이론대로 스케일한다** — 치료시간 2배 → 대기 중위 51.0 → 108.1분(**2.12배**).
  최적 팔의 대기 후 진입 비율이 용량에 단조(×0.5 18.1% > 기준 11.5% > ×2.0 6.5%).
  §스케일링의 지수 측정과 **독립적으로 같은 결론**에 도달한다.
- 전원(diversion)은 전 조건 0건. 도착 게이트(수술실수+병상수 ≈ 14)는 실제로 안 걸린다.

⚠️⚠️ **계측 버그 두 개를 먼저 잡아야 했다 — 방법론 교훈.** 분해 항등식을 가드로 걸었더니
① 계측을 `reset` 후에 켜서 초기 구조환자 누락(잔차 0.717), ② **시뮬 trace 에 병상 대기 → 수술실 진입
경로의 `care_start` 기록이 아예 없어서 기다린 환자만 정확히 누락**(잔차 0.575)이 드러났다.
그 상태로 잰 두 결과가 전부 허위였다 — "처치 환자 97.8% 가 대기 0", "부하항이 진입률을 27%→49% 로 올린다".
**항등식 가드가 없었다면 두 오류를 논문에 실었을 것이다.**

**진짜 자유 상수는 하나** — 대기시간 할인율. 규칙이 만드는 구성가중 서비스시간 30.44분 대비
Q 보간최적 18.57분 = **0.610**, 생존확률을 직접 쓰는 S 가족의 배율 최적 = **0.62**.
두 파라미터화가 독립적으로 같은 값을 복원한다. ⚠️최초 가설("생존곡선이 늦은 지연을 덜 벌한다")은
**S 가족이 생존곡선을 쓰는데도 할인이 필요**하다는 사실로 반증됐다.

**교사 대조** — 물리 조건별 재학습 PPO(10M, v19 레시피 동일)와 같은 좌표·시드 비교.
Q 는 START-LB3 를 +0.0195~+0.0648, 현행 CARD 를 +0.0045~+0.0097 로 전 조건 앞서고,
**재학습 교사도 6조건 중 5곳에서 유의하게 앞선다**(환자 50만 −0.00101 열세).
재학습의 가치는 이동이 클 때만 나타난다(capa05 +0.0088, amb10 +0.0031; amb20·n200 은 악화).
⚠️**정보 비대칭** — CARD 계열은 병원 실시간 점유를 쓰고 v19 field obs PPO 는 뺐다.
깨끗한 비교는 Q vs 현행 CARD(+0.0068, 같은 정보) 와 Q vs START-LB3(+0.030) 이다.

**★재현율과 성능이 역전한다(모방≠성능 6번째)** — 교사 결정 222,000건 선택집합 top-1 재현율은
K 0.3146 > 최근접 0.2372 > T 0.2033 > H 0.1959 > **Q 0.1792** 로 폐루프 순서와 정확히 반대다.
v17 이 트리 8팔에서 본 재현율–PDR r=−0.925 는 **같은 가설공간 안에서만** 성립한다는 정밀화다.

**감사에서 잡은 것 (전부 조치)**
1. 판정선 정정 — 0.00114(학습 시드)는 규칙에 없는 잡음원. 규칙 실험 판정선은
   **CRN paired 두 팔 차이 95%CI = 0.00053**(실측). 좌표 재표집(절대 PDR)은 0.01249 로
   **다른 좌표셋 수치와 병치할 때만** 쓴다.
2. 28 설정은 독립 아님 — 설정별 Δ 쌍상관 r̄ 0.198 → **n_eff ≈ 4.4**. "28 독립 조건" 표현 금지.
3. 격자 해상도 — "정확히 불변" 금지. 로그축 포물선 보간 후 기울기+CI 로 판정.
4. **★좌표 누수** — `tradeoff250` 은 v19 가 판정 전용으로 선언한 `test750` 의 **정확한 1/3**(250/250).
   조치: `budget750`(test750 과 교집합 0)에서 재도출 → **H 12 · Q 18 이 정확히 재현**되고
   배포형 paired 도 유지(Q18 vs K12 +0.00663±0.00041, 450/299/1). K 만 12→10 한 스텝 이동.
   **누수는 실재했지만 결론을 만들지 않았다.** 앞으로 λ 선택은 budget750 에서만.

⚠️ 이 표는 tradeoff250/budget750 좌표셋이라 §4-2 의 두 표와 직접 비교 금지.

### 4-4. v21 결과 — 정보수준 × 함수형 격자 (budget750, 750좌표 × 10시드)

부하 신호의 **출처**와 벌점 **함수형**만 바꾸고 셀마다 λ 를 다시 골랐다. 신규 52팔 39만 에피소드.

| 부하 신호 (정보수준) | 통신 | 선형 `q` | 초과분 `(q+1−c)⁺` | 초과분÷c |
|---|:--:|---:|---:|---:|
| **I3** 병원 재고 + 내 이송기록 | 필요 | 0.146806 (9) | 0.141433 (12) | **0.138973 (18)** |
| **I2** 병원 재고만 | 필요 | 0.223919 (18) | 0.231391 (27) | 0.231872 (32) |
| **I1a** 지금 가는 중만 | 불요 | 0.149033 (12) | 0.147015 (21) | 0.146836 (26) |
| **I1b** 내가 보낸 누적 | 불요 | 0.147581 (9) | 0.142789 (9) | **0.139754 (18)** |
| **I0** 부하 무시 | 불요 | — | — | 0.259411 |

- **통신 비용은 +0.000781 ± 0.000147**(I3 → I1b, 180/562/8) = 격차의 0.6%, 회복률 99.4%.
- **I2 는 통신이 있는데도 +0.0929 붕괴**(746/4/0). 기전 = 정보 지연 → **쓴 병원 수 5.8~6.5곳**
  (부하 무시 4.96 / I1b 14.9 / I3 14.5). 병원 census 는 도착·접수 뒤에야 올라간다.
- **λ 는 신호 출처와 무관**: I3·I1b 모두 18분/명 (S/c 유도와 일치).
- **함수형 손실이 통신 손실보다 크다**: 나눗셈 제거 +0.00283(3.7배), 선형화 +0.00497(6.5배).
- 함수형 순서는 I3·I1b 에서 재현되지만 **I2 에서는 역전**한다 — 신호가 틀리면 함수형 개량 무의미.

---

## 5. 현재 좌표 — 무엇이 참이고 무엇이 열려 있나

**확립된 것(v21 기준; 과거 수치에는 해당 조건 표시)**

- test750에서 field PPO는 기존 Full64/LB 계열을 앞선다. 최신 Q18은 최강 PPO_SIDO와 실용적 동률이며,
  P18은 누적발송 장부로 정보 요구를 낮춘 카드다. “모든 규칙보다 RL이 우세”는 현재 결론이 아니다.
- v3~v7의 반응형 정책에서 룩어헤드 흡수는 반복 음성이었다. 당시 오라클 도달률 약57%를
  v19 정책의 상한으로 옮기거나 일반적 불가능성으로 주장하지 않는다.
- 지역화 여지12.4%와 변수선택 기여5.5%는 v18의 규칙 가족·평가축에서 측정한 값이다.
- v19 슬롯별 정규화 수정은 이전 PPO와 CARD의 순서를 뒤집었다. v20~v21에서는
  대기행렬형 규칙으로 함수형을 개선한 결과를 그 교사와 다시 비교했다.

**열려 있는 것**

1. **임계값의 전이와 남은 할인 기전** — v20 물리조건 스윕은 완료됐고 budget750 재도출은
   **당시 λ 축만** 옮겼다(§4-3). `yhold`·`red`·`extreme`·`treat` 스테이지는 `tradeoff250`
   (= test750 부분집합)에 남아 있었고 **2026-09-11 v22 캠페인에서 36조건 전부 budget750으로
   재도출됐다**(`agent_docs/research.md` §v22가 정본).
   함수형 보정 후 작아진 전이 후회와 유효 서비스시간 할인 약0.61의 원인을 구분한다.
   검증한 물리조건 밖에서의 정확한 불변성은 입증되지 않았다.
2. **증류 트리의 낮은 fidelity(0.33)** — 이것을 "실패" 로 둘지 "행동일치도는 성능 대리변수가
   아니다" 라는 결과로 재정의할지. v13 의 동률 사례와 v17 의 무차별 진단(후보 중위 47개인데
   perplexity 8.2, 1·2순위 확률차 중위 0.087, 53%가 0.1 미만)이 후자를 지지한다.
3. **시뮬 외부 타당성** — 고속도로 사고DB 검증에서 AMB 응답 동역학은 부분 검증됐으나
   **UAV 200km/h 는 대응 데이터 0개로 전면 미검증**이다.
4. **목적함수의 윤리** — "항상 Yellow 우선이 최적" 은 총 예방가능 사망 최소화의 귀결이다.
   생존곡선 기울기 교차점(|p'_Red| = |p'_Yellow| 가 되는 시각 **71.5분**)으로 설명 가능하지만
   논문에서는 명시적으로 다뤄야 한다.
5. **시드 반복** — v19 Track1/Track2 판정은 시드 각 1개다(효과크기 0.0009 < 잡음바닥 0.0011).

---

## 6. 이 문서의 유지 규칙

- 새 버전이 끝나면 §1 에 막을 추가하고, 기각이 나오면 §2 에, 새 검정 관례가 생기면 §3 에 적는다.
- 수치를 옮길 때는 **판정셋·에피소드 수·시드·하네스**를 함께 옮긴다. 그것 없는 수치는 비교 불가다.
- §4-1 의 경계를 넘는 비교를 하려면 옛 산출물을 새 하네스로 **재측정**해야 한다.
