# MCI_UAV
재난 시뮬레이션 내에서 강화학습을 활용한 UAV Triage 연구

## Clone

ml-agents가 서브모듈로 포함되어 있어 함께 받아야 합니다.

```bash
git clone --recurse-submodules https://github.com/bbcc1017/MCI_UAV.git
```

이미 클론한 경우:

```bash
git submodule update --init --recursive
```
