# 연구 상태와 판정 계약

확인: 2026-09-11, v21 완료 산출물 + v22 budget750 정본. 이 문서는 현재 작업의 출발점이다.
버전별 상세 수치·기각 경위는 [RESEARCH_HISTORY.md](../RESEARCH_HISTORY.md),
v3~v5 전문은 [RESEARCH_LOG.md](../RESEARCH_LOG.md)에 둔다.

## 목표와 발전 방향

목표는 한국 MCI에서 AMB와 UAV로 구조된 환자를 이송할 때 예방가능 사망을 줄이고,
전국에 통하는 정책에서 현장 지휘자가 사용할 물리단위 규칙을 도출하는 것이다.
현재 메인트랙은 **PPO 교사 → 결정 로그 분석 → 규칙 함수형·임계값 도출 →
튜닝 좌표 폐루프 선택 → 독립 좌표 판정 → 기전·정보요구 검증**이다.
PPO 성능만 높이거나 행동 모방 정확도만 높이는 것이 최종 목표는 아니다.

| 시기 | 방향을 바꾼 근거 | 현재 작업에 남는 교훈 |
|---|---|---|
| v1~v2 | 로그에서 만든 LB-T가 초기 RL을 추월; 부하 관측·pointer로 재역전 | 강한 설명가능 기준선과 관측 계약을 먼저 확인 |
| v3~v7 | NCRP는 개선, 행동 BC·가치학습·CRR로 계획 이득 흡수는 반복 음성 | 같은 가설 재학습보다 정보·결정시점 계산의 차이 검토 |
| v6~v12 | H 패딩 채택; 3원·잔차·GOPT head 음성; attention 0블록 채택 | 병원수 가변 지원과 표현력 확대는 별개 |
| v10~v15 | 후보랭킹 CART/GBDT, 낮은 fidelity로도 좋은 폐루프; 포트폴리오 blind 음성 | 행동일치도는 PDR의 대리변수가 아님 |
| v16~v18 | START-LB3 비교군 정립, CARD 채굴, 지역화 이득 제한 | 누적발송 상한에서 연속 부하 벌점으로 바뀐 효과가 큼 |
| v19 | 슬롯별 VecNormalize를 제거한 현장 관측으로 PPO 재역전 | 같은 물리량은 모든 병원 슬롯에서 같은 눈금으로 읽혀야 함 |
| v20~v21 | 시간+대기행렬형 규칙, 튜닝 누수 정정, 30시드·최강 교사 감사 | 정보량보다 갱신 시점; Q18은 최강 교사와 실용적 동률 |
| v22 | 현장 어시스트 설계·실행시간 계측, budget750 36조건 재도출 | λ는 관측량의 일차 함수이고 공식이 시뮬 최적과 같은 배포값을 준다; 남은 자유도는 등급 축 하나 |

기각은 **당시 데이터·예산·정보·구현 조건에서의 결과**다. 수학적 불가능성으로 일반화하지 않는다.
재시도할 때는 달라진 가정과 성공/중단 기준을 먼저 적고 작은 개발 실험으로 검증한다.
특히 옛 `p_sent` 기각은 행동회귀에 근거한 판단으로 **v18~v21에서 뒤집혔다**.
옛 “h 증액 무효”도 v11의 직접 paired 비교로 정정됐다.

## 현행 데이터 역할

매니페스트는 `scenarios/manifests/` 아래에 있다. 경로 문자열/키만 보지 말고 실제 좌표를 비교한다.

| 파일 | 역할 |
|---|---|
| `sigungu30_train6000_manifest.json` | 시군구250 × 24좌표, 학습 전용 |
| `sigungu30_budget750_manifest.json` | 시군구250 × 3좌표, 임계값·예산·후보 선택 |
| `sigungu30_test750_manifest.json` | 시군구250 × 3좌표, 선택 후 판정 |
| `sigungu30_points.json` | 원 좌표 원장; 생성기의 OSRM snap 거리 게이트도 확인 |
| `v19/tradeoff250_manifest.json` | test750의 250좌표 부분집합. 튜닝에 사용하면 누수 |

세 주 집합의 정확 좌표 교집합은 0이다(2026-09-09 전수 재확인).
test750은 이미 여러 버전에서 열어 본 판정셋이다. 이후 결과를 보고 새 가설·정책을 선택했다면
이를 완전히 새 blind test라고 부르지 않는다. 새 확증에는 선택 이력을 밝히고 필요 시 새 좌표를 확보한다.
같은 시군구의 새 좌표 검증은 **unseen-coordinate 일반화**이며, 미학습 시도·전국 모든 재난의 검증과 다르다.

과거 분할은 재현용으로 보존한다. v10 random4 1000/대표점250, v3~v9 시도17·seed11000,
v10 Full64 1000ep(seed0..999)를 현재 test750 규약에 섞지 않는다.
대표점250은 과거 모델 학습 이력과 OSRM snap 문제가 있어 현재 판정축을 대체하지 못한다.
`plan1nat_manifest.json`·`national_train.json`은 삭제된 경로를 참조하는 레거시다.
`scoreboard/v10_protocol.json`·`v12_protocol.json` 등은 **해당 버전**의 정본이다.
v19 run의 `OTHER_OR_LEGACY`/`scoreboard_eligible=false`는 v10 태거 결과이므로 v19 무효 판정이 아니다.

## 현행 정책과 근거

v21 test750 × seed0..29, 정책당 22,500ep, `PDR_woG`(낮을수록 좋음).

| 정책 | PDR | 구성/역할 |
|---|---:|---|
| START_LB3 | 0.167356 | 사후선택 없는 설명가능 기준선 |
| CARD_K12 | 0.144985 | 이전 거리+선형 부하 규칙 |
| PPO_NATIONAL | 0.139738 | v19 전국 단일 교사, 학습 seed0 |
| CARD_P18 | 0.139207 | 현장 누적발송 `p_sent`를 쓰는 권장 카드 |
| PPO_SIDO | 0.138839 | v19 광역시도17 정책, 계열별 학습 seed0 |
| CARD_Q18 | 0.138445 | 병원 census+in-flight를 쓰는 카드 |

Q18−K12 개선은 **+0.006540 ±0.000318**, W/T/L **626/122/2**다.
Q18−PPO_SIDO 개선은 **+0.000393 ±0.000340**로 지역축 CI만 보면 유의하지만,
v21 실용 판정선 0.00053 미만이고 학습 seed 반복이 부족하다.
채택 문구는 **“최강 교사와 실용적 동률”**이다. 통계적 동등성 검정을 통과했다는 뜻은 아니다.
`PPO_NATIONAL`만 비교해 “교사를 추월했다”고 쓰지 않는다.

출처/재계산:

- `tools/v21_infoladder_report.py judge`와 `audit` (기존 CSV 읽기, 시뮬 재실행 없음).
- `results/scoreboard/v20/theory/test750_final.csv`(seed0..9),
  `results/scoreboard/v20/final30/test750_rules_s10_29.csv`.
- `results/scoreboard/v21/final/test750_info_s0_29.csv`,
  `results/scoreboard/v19/{ppo,rules,cards}_test750.csv`.
- 로컬 상세: `docs/v21_정합성감사_정보수준카드_2026-09-08.md`,
  `docs/v20_임계값_일반화_방법론_2026-09-06.md` (다른 박스에서는 존재 확인).

## CARD의 정확한 의미

구현: `src/rl_src/v17_field_rules.py`의 `make_field_card_time_policy`·`_load_vector`.
명시 스펙: Q18=`cardt:18,6.6,0,hingerate`, P18=`cardt:18,6.6,0,hingerate_psent`.
함수 기본 인자는 채택값과 다를 수 있으므로 항상 전체 스펙을 저장한다.

목적지 점수는 `도달시간(분) + λ × max(0, q+1−c)/c`다.
도달시간은 평균 이송시간+인계시간, `c`는 수술실수(치료 서버수), Q의 `q`는
`occ+in_flight`, P의 `q`는 `p_sent`다. rate 형태에서는 이미 c로 나눴으므로
남은 λ는 **유효 서비스시간 계수**다. hinge 형태의 계수 `S/c`와 혼동하지 않는다.
관측된 할인율 약0.61과 λ18은 실험 근거가 있는 근사이며 보편적 정확 상수는 아니다.

`yhold=0`은 이송 가능한 Yellow가 있으면 Yellow를 우선한다.
두 수단 모두 적격일 때 Red는 최소 도달시간의 AMB−UAV 이득이 6.6분을 넘으면 UAV,
Yellow는 AMB를 쓴다. 목적지는 확정한 등급·수단의 hard mask 적격 후보에서 고른다.

P18의 “통신 불요”는 **부하 점수 입력**에 대한 표현이다. 평가 하네스는 공통 `occ` 안전마스크를
사용하므로 시스템 전체의 완전 무통신까지 증명한 것은 아니다. `field` PPO도 마스크가 가진 정보를
별도로 감사해야 한다. occ만 쓰는 카드의 실패는 갱신 지연으로 같은 병원에 반복 발송하는 현상이다.

## v22 현장 어시스트 — budget750 정본 (2026-09-11)

조건 36개 × 750좌표 × seed0..9 = **7,657,500 에피소드**, 원본 `src/sim_src` 코어, 실패 0.
드라이버 `tools/exp_drivers/run_v22_field.sh`, 리포트 `tools/v22_field_report.py`,
산출 `results/scoreboard/v22/retune/` + `results/field/canonical/`.
v20 `theory` 스테이지(`yhold`·`red`·`extreme`·`treat`)는 `v19/tradeoff250`(= test750 부분집합)을
썼으므로 **선택 근거는 이 재도출본이 정본**이고 v20 theory 수치는 잠정으로 강등한다.

**① λ는 평균 서비스시간의 일차 함수다.** 치료시간 축 보간 최적(Q족, `hingerate`):

| 치료시간 배수 | 0.5 | 0.75 | 1.0 | 1.5 | 2.0 | 3.0 | 4.0 |
|---|---:|---:|---:|---:|---:|---:|---:|
| λ\* | 8.22 | 14.01 | **18.97** | 29.29 | 42.42 | 64.46 | **85.11** |

로그-로그 기울기 **+1.121**(이론 +1, 격자끝 없음 n=7). H족 +1.127.
명부에서 서비스시간을 직접 읽는 S족은 **+0.137 = 사실상 평평**(재튜닝 불요).
사전등록한 `λ*(ts=4) ∈ [79,98]` 예측이 측정 85.11로 확인됐다.

**② 공식이 시뮬 최적과 같은 배포값을 준다.** `λ = 18.97 × 치료시간배수`가 6조건 **전부**에서
시뮬 최적과 같은 격자 팔을 고른다(0.5→Q10 · 0.75→Q14 · 1.5→Q32 · 2.0→Q40 · 3.0→Q65 · 4.0→Q85).
격자 해상도 ±30% 안의 일치라는 단서를 붙이되, 배포는 숫자 하나를 정하는 일이므로
**λ 결정에 폐루프가 필요하지 않다.**

**③ 봉투 밖 손실은 λ 재튜닝만으로 전부 회수된다.** `MCI_TREAT_SCALE=4.0`에서 고정 `Q18` 손실
**+0.02775 ± 0.00169 (W/T/L 619/130/1)**. Q 응답이 내부 최소점을 가지므로
(50→0.15989 · 65→0.15822 · **85→0.15784** · 110→0.15818 · 140→0.15901) 회수율 **100%**다.
함수형 교체는 불필요하다. ⚠️tradeoff250 잠정치의 "회수율 94.9% · 잔여는 무튜닝 S족의 몫"은
**격자 절단의 산물이었다** — 이 문장으로 대체한다.

**④ 재튜닝이 필요한 축은 치료시간 하나다.** 배포 상수 대비 그 조건 최적의 이득:
`ts40` +0.02775 ≫ `n500` +0.00137 · `amb1` +0.00089 · `capa03` +0.00067 · `amb3` +0.00054 ·
`vamb20` +0.00031 · `uav0` 0. 등방 "봉투 거리"(정식조건 대비 log2 배율 편차의 L2 노름) 가설은
**기각**이다(Spearman ρ −0.169, p=0.60, n=12). 치료시간 축 안에서는 강한 단조
(**Pearson r +0.928 · Spearman ρ +0.991, p=1e-5, n=7**)이고 비대칭이다
(느려짐 +0.02775@×4 ≫ 빨라짐 +0.00288@×0.5). 판정선 교차는 `|log2 배수| = 0.58` = **1.5배**.

**⑤ 등급 축은 0이 아니고 조건에 따라 움직인다.** 채택값 `yhold=0` 대비 그 조건 최적의 이득이
12조건 중 **7곳에서 판정선 초과**: `capa20` Y8 +0.00154 · `ts05` Y8 +0.00121 · `n50` Y8 +0.00091 ·
`vamb70` Y8 +0.00081 · `amb5` Y2 +0.00074 · `vamb100` Y8 +0.00067 · `amb10` Y2 +0.00064.
기준조건은 Y4 +0.00041로 유의하나 판정선 미만이다. 패턴은 **여유가 크면 Red 우선을 8명까지,
차량이 희소하면 2, 느리거나 빡빡하면 0**이다. 이 축에는 스케일링 공식이 나오지 않았다.

**⑥ 두 축은 파라미터 선택에서 독립이다.** (λ × yhold) 결합 격자 5조건 중 4곳에서 결합 최적이
축별 단독 최적과 **좌표까지 일치**(base (18,4) · capa20 (18,8) · ts05 (10,8) · ts40 (26,0)).
어긋난 `vamb100`도 성능차 +0.000073 ± 0.000143 = 동률. ⚠️**결정 순서의 의존성은 별개다** —
목적지 점수의 도달시간 벡터는 수단이 확정돼야 정해진다. 실무 함의: 현장 후보를 곱셈이 아니라
덧셈으로 만들어 같은 예산에서 시드를 늘릴 수 있다.

**⑦ 명부 직독형(S족) 카드의 봉투 안 비용.** `S0.62` vs `Q18` = **−0.000714 ± 0.000194** (9/650/91) =
판정선 1.3배로 **명부 직독형이 열세**. 최적 `wait_scale` 0.62가 누수 없는 좌표셋에서 재현됐다.
⚠️이 형태를 "무튜닝"이라 부르지 않는다 — `wait_scale` 자체가 튜닝 숫자다(⑫).
같은 스테이지 값: Q18 0.138973 · S0.62 0.139687 · P18 0.139754 · K12 0.145606 · START_LB3 0.168331.

**⑧ 실행시간.** 원본 코어 1코어 median cpu ms/ep = N50 36.1 · N100 66.6 · N200 137.5 ·
N300 209.6 · N500 343.1. 비용은 사고규모 N에 **정확히 선형**(로그-로그 0.989), 결정당 1.67~1.75 ms
일정, 정책 계산은 총비용의 27~29%뿐, 지형 영향 약 2%로 무의미.
지휘차 24코어·3분이면 N=500에서도 약 12,600 에피소드. 출처 `results/field/timing/n_scaling.json`.
프로덕션 처리량 실측 **640 ep/s @56워커**.

**⑨ 병목은 계산이 아니라 온라인 선택의 통계다.** ⚠️**이 항은 ⑩에서 정정됐다 — 반드시 함께 읽는다.** 1좌표·39후보·내부4시드에서
`selection_regret`(= `outer(선택) − outer(외부 오라클)`) **+0.0114**로, 그 카드가 기준 대비
주장하는 이득(+0.0089)보다 크다. 내부 top-1이 외부에서 14/39위였다.
`outer − inner` 형태의 혼합 지표는 시드집합 난이도 이동(−0.0117)과 상쇄돼 −0.0002를 보고하므로
**선택 편향을 가린다** — 반드시 `selection_regret`과 `level_shift`를 분리해 본다.
오라클도 M 시드 추정이므로 paired 95%CI를 병기하고, `regret < ci95`면 곡선의 바닥이다.

**⑩ 예산 곡선 본실험 (2026-09-11, budget750 60좌표 × 32팔 × 722k 에피소드).**
내부 seed 0..127(선택) / 평가 seed 1000..1059(M=60, 서로소). CRN 시드 **접두사 재사용**으로
K 격자 14점이 K=128 한 번 값에서 나온다. `regret = E_eval[선택] − min_c E_eval[c]`.
`min` 이 잡음 추정이라 regret 은 상향 편향이고 그 바닥은 **K=128 값**으로 읽는다
(base joint 0.00049 · ts40 joint 0.00032 = 판정선과 같은 크기).
도구 `tools/v22_field_report.py budgetcurve`, 산출 `results/scoreboard/v22/budgetcurve/`.

0-에피소드 기준선 둘. **공식 카드**는 `λ = 18.97 × 치료시간배수` 를 격자에 반올림한 팔이다.

| 조건 | 고정 카드 `Q18Y0` | 공식 카드 | 회수율 |
|---|---:|---:|---:|
| base (ts=1.0) | +0.00129 ±0.00042 (중위 .00065, 판정선 초과 53%) | `Q18Y0` 동일 | — |
| ts40 (ts=4.0) | +0.03076 ±0.00611 (판정선 초과 **100%**, eval 순위 22/32) | `Q85Y0` **+0.00105 ±0.00036** | **96.6%** |

**★ 공식 한 줄이 에피소드 0개로 봉투 밖 손실의 96.6% 를 회수한다.**

남은 몫은 등급 축이다. ts40 에서 λ 를 공식으로 고정하고 `y ∈ {0,2,4,8}` 4팔만 K 시드로 고르면
공식 카드 대비 좌표 paired 개선이 K=3(**12 에피소드**)부터 유의해진다 —
+0.00034 ±0.00021 (W/T/L 23/32/5), K=8 에서 +0.00045 ±0.00022 로 포화.
⚠️**개선폭이 판정선 0.00053 미만이다.** 정직한 문장은 "유의하지만 판정선 미만" 이다.

동일 에피소드 예산에서 탐색 폭 비교(ts40, regret 평균):

| B(ep) | joint 32팔 | additive 12팔 | yonly 4팔 |
|---:|---:|---:|---:|
| 32 | K=1 0.00390 | K=2 0.00193 | K=8 **0.00060** |
| 64 | K=2 0.00163 | K=5 0.00108 | K=16 0.00065 |
| 128 | K=4 0.00094 | K=10 0.00069 | K=32 0.00067 |
| 256 | K=8 **0.00054** | K=21 0.00052 | K=64 0.00063 |
| 512 | K=16 **0.00040** | K=42 0.00042 | K=128 0.00052 |

**★ 예산이 작을 때만 후보를 좁히는 것이 이긴다**(B=32 에서 6.5배). B≥256 에서는 세 탐색이
바닥으로 수렴하고 joint 가 근소하게 낫다. **그리고 현장 예산은 큰 쪽이다** — 24코어·3분이면
N=500 에서 12,600 ep, N=100 에서 64,800 ep 로 **바닥에 닿는 데 필요한 256~512 ep 의 25~125배**다.

→ **⑨ 의 "병목은 통계" 를 정정한다.** 1좌표·39후보·내부 4시드에서 본 `selection_regret` +0.0114 는
**현장 예산과 무관한 극소 예산 영역의 현상**이었다. 실제 현장 예산에서는 계산도 통계도 남지 않는다.
**남는 병목은 입력이다** — 치료 회전율이라는 숫자 하나가 봉투 밖 손실의 96.6% 를 결정한다.
부수 관찰: `MCI_TREAT_SCALE=4.0` 은 `cpu_ms/ep` 를 거의 안 바꾼다(70.5 → 69.6). 치료시간은
지연이지 계산이 아니므로 ⑧ 의 N-선형 비용 모델에 이 축은 들어가지 않는다.
⚠️ 표의 벽시계 환산은 실제 수집 4회 대조에서 **15~21% 낙관**이므로 하한으로 읽는다.

**⑪ 입력 오추정 강건성 (E8, 재분석).** 공식의 입력인 치료 회전율을 배율 `e` 만큼 틀렸을 때.
`tools/v22_misspec_report.py grid`, budget750 750좌표×10시드, 7 조건 × 7 오차.
- **허용 오차구간**(regret ≤ 판정선): 전 조건 공통 **e ∈ [0.85, 1.24]**, 조건평균 [0.78, 1.45].
- **minimax 전략 비교**(조건간 최악 regret): 고정 `Q18` **0.02775** · 공식 오차없음 0.00052 ·
  **공식 ±20% 0.00052**(판정선 이내) · 공식 ±50% 0.00206 · 공식 ×0.5~2.0 0.00808 ·
  S0.62 0.00077. **2배까지 틀려도 고정 카드보다 3.4배 낫다.**
- **비대칭 — 과대추정이 과소추정보다 싸다**(예측규칙 46/3 적중, 빗나간 3건이 전부 과대추정 쪽).
  현장 휴리스틱으로 "모르면 크게 잡아라" 가 안전 방향이다.
- ⚠️ **정상 조건(ts=1.0)에서는 조정 자체가 해롭다** — e≠1 이 전부 유의패다. 조정하지 않을 근거도 필요하다.
- ⚠️ S0.62 의 '오추정 위험 0' 은 **시뮬의 성질**이다. S족은 병원 명부의 서비스시간을 직접 읽고
  `MCI_TREAT_SCALE` 이 그 명부를 스케일한다. 현실 명부에는 명목값만 있으므로 이 행은 현실 주장이 아니다.

**⑫ 최소 충분 파라미터 (E2, 재분석).** `tools/v22_sufficiency_report.py`.
카드에 **남는 숫자 5 + 구조 2**, **뺀 것 1**.
- 남음: `λ`(공식이라 자유 파라미터 아님) · 부하항 함수형 `hingerate`(대체형 비용 hinge1 +0.00246 ·
  linear +0.00783 · zero +0.12044) · `yhold`(12조건 중 7곳 판정선 초과) ·
  `red_gain_min` 6.6(최대 격차 +0.00066±0.00021, 겨우 남는다) · 부하 신호 출처(정보수준은 서열이 아니다 —
  occ 단독 −0.09290 이 최악).
- **뺌: 지역화 파라미터표.** 오라클 상한 L3 +0.00165/L2 +0.00081 인데 **정직한 시드분할에서
  L3 −0.00015±0.00026 · L2 −0.00001±0.00024**. 오라클 이득이 전부 선택 잡음이다.
- ★**S족을 "무튜닝" 이라 부르면 안 된다.** `wait_scale` 이 튜닝 숫자다 —
  `S0.62 − S1.0 = +0.00160±0.00027`(13조건 전부 판정선 초과, 이론값 1.0 이 열세).
  이 문서의 이전 서술에서 "무튜닝 S족" 표현을 쓴 곳은 **"명부 직독형 S족"** 으로 읽는다.

**⑬ 심각도 프리셋 표 (E5, 재분석).** `tools/v22_preset_table.py`. 물리조건 25개.
- **조건별 조회표(B)는 사실상 오라클**이다(서로소 시드 승자의 저주 검사: 22조건 중 14곳 일치,
  최대 선택후회 +0.00031±0.00036).
- **공식형 카드(A)는 λ 는 옮겨지고 y 는 옮겨지지 않는다.** y 를 지휘관 가용 입력(여유 S·이송률 R)의
  임계 규칙으로 쓰고 **LOO(보류조건 예측)** 로 정직하게 재면 `vamb100` +0.00067±0.00015 ·
  `vamb70` +0.00081±0.00023 · `base` +0.00041±0.00018 을 잃는다(참값 y\*=8 인데 0 예측).
  in-sample 손실 = LOO 손실이라 **규칙이 in-sample 에서도 안 맞는다**. → y 의 공식화는 실패다.
- **인쇄 가능성**: λ 를 5의 배수로 반올림해도 허용대 안이고 직접 측정된 셀에서 손실 ~0.
  카드에 `λ = 19 × 배수` 를 적고 5 단위로 읽어도 된다.

**⑭ 2임계 밴드와 등급 게이트 (E1, budget750 신규 4조건 × 28팔 × 84만 ep).**
`cardt2:` 스펙 신설(`src/rl_src/v17_field_rules.py`·`v17_rule_eval.py`, 커밋 `cf036d0f9`).
회귀 게이트: `y=x,r=0,gate_uav=on,coupled=off` 가 기존 `cardt:` 와 **비트동일**
(에이전트 2단계 1,200값 + 독립 재검증 5좌표 200값, 전부 불일치 0).
- **밴드는 탈락.** 최선 밴드 − 최선 단일임계 = base +0.000000 · capa20 +0.000080±0.000131 ·
  ts05 +0.000000 · vamb100 +0.000039±0.000067. **전 조건 동률, 최대가 판정선의 1/7.**
  사용자가 요청한 2임계+교대 형태는 **파라미터 값을 벌지 못한다** → 카드는 단일 임계를 유지한다.
- ★**대신 기전이 하나 나왔다 — 등급 이득은 'UAV 현장 대기' 게이트에 조건부다.**
  `gate_uav` 를 떼면 `yhold` 효과의 **부호가 뒤집힌다**:

| 조건 | 게이트 on: Y8−Y0 | 게이트 off: Y8−Y0 |
|---|---:|---:|
| base | +0.000061 (동률) | **−0.001791 (악화)** |
| capa20 | **+0.001540** | **−0.001370** |
| ts05 | **+0.001206** | **−0.000983** |
| vamb100 | **+0.000673** | **−0.000450** |

  같은 `yhold` 에서 게이트 on 이 off 를 +0.00065~+0.00291 앞선다(전부 판정선 초과).
  즉 **"Red 우선" 은 UAV 가 현장에 대기 중일 때만 이득**이고, 게이트는 헤드룸을 막는 장치가 아니라
  **헤드룸을 만드는 장치**다. 현행 카드의 `and uav_here` 구조가 기전으로 정당화된다
  (UAV 의 주역할이 AMB 대기열 흡수라는 v17 마스크 감사와 정합).

**⑮ 결합 점수 — 목적지×수단은 결합해야 한다 (E1, budget750 2조건 × 7팔 × 10.5만 ep).**
사용자의 원래 통찰은 "`mode` 가 도달시간을 바꾸고 그 도달시간이 `destination` 점수의 첫 항이므로
축이 독립이 아니다" 였다. 그 가설을 두 자리에서 따로 쟀고 **답이 갈린다**.

- **파라미터 선택에서는 독립**(⑥). λ 와 yhold 를 따로 골라도 결합 격자의 최적에 도달한다.
- **결정 구조에서는 결합해야 한다.** 현행은 class → mode → destination 순차이고 수단을 최소 ETA 로
  먼저 고정한다. 결합형은 적격 `(수단, 병원)` 쌍에서
  `t_m(h) + λ·(q+1−c)⁺/c + μ·1[m=UAV]` 를 한 번에 argmin 한다(`cardt2:…,coupled=on`).

| μ(UAV 기회비용, 분) | base Δ vs 순차 | ts40 Δ vs 순차 |
|---:|---:|---:|
| 0 | +0.000256 ±0.000139 | **+0.003782 ±0.000618** (150/600/0) |
| 3 | +0.000481 ±0.000136 | **+0.003622 ±0.000594** |
| **6.6** | **+0.000513 ±0.000132** (65/684/1) | +0.003058 ±0.000540 |
| 10 | +0.000440 ±0.000130 | +0.002322 ±0.000473 |
| 15 | +0.000267 ±0.000121 | +0.001413 ±0.000402 |
| 25 | −0.000107 ±0.000104 (열세) | +0.000144 ±0.000298 (동률) |

- **봉투 안에서는 판정선 경계**(최선 +0.000513 vs 판정선 0.00053, 유의하나 미만).
- **봉투 밖에서는 판정선의 7배**(+0.003782). **등급 축이 주는 어떤 값보다 크다**(yhold 최대 +0.00154).
- ts40 의 최적 `μ` 가 **0** 이다 — 치료가 느려지면 대기 항이 지배해서 UAV 를 아낄 이유가 사라지고
  "가장 빨리 덜 붐비는 병원에 닿는 (수단,병원) 쌍" 이 답이 된다.
- → **카드의 목적지·수단 두 단계를 하나의 argmin 으로 합친다.** 순차 형태는 봉투 밖에서 손해다.
  ⚠️결합형은 `Yellow→AMB` 제약을 걸지 않으므로 이 Δ 는 "결합 + Yellow 수단 자유" 의 합산 효과다.

**⑯ 링크 2 재적합 — 현행 교사 결정로그의 변수 중요도 (E6, 시뮬 재실행 0회).**
`tools/v22_link2_importance.py`, v19 field obs 교사 `results/scoreboard/v19/cards/dec_national.npz`
(222,000 결정 · 후보 10.97M 행 · 43특징) 에서 40,000 선택집합 표본. 배선 확인으로 정본 값 재현
(λ=5.7251 vs 5.6227 · top-1 0.3039 vs 0.3073). 적합 138분.

**★전진선택이 카드의 두 항을 스스로 1·2순위로 골랐다.**

| 단계 | 고른 특징 | pseudo R² | Δ | 재현율 | Δ |
|---|---|---:|---:|---:|---:|
| 1 | `eta_raw_min` (도달시간) | 0.3449 | +0.3449 | 0.1901 | +0.1643 |
| 2 | `hingerate_capa` (부하 초과분÷수술실수) | 0.4221 | +0.0772 | 0.2972 | +0.1071 |

카드 점수 `도달시간 + λ·(q+1−c)⁺/c` 의 두 항이 **데이터가 스스로 뽑은 상위 둘**이고, 그 둘만으로
전체 28특징(pseudo R² 0.519 · 재현율 0.460)의 **81% / 65%** 를 가져간다. 카드 변수집합 전체의
재현율은 0.3275(전체의 71%). ⚠️`--kmax 2` 로 잘랐으므로 3단계 이후는 미측정이다.

**중요도 ≠ 정보이득이 같은 표에서 보인다.** `eta_x_rho` 는 표준화 계수 순위 **2위**인데 gain 순위
19위·LOO ΔpseudoR² 0.00017 이고, `eta_rank` 는 계수 8위인데 **LOO Δ재현율 −0.0077(음수)** 이다.
반대로 LOO Δ재현율 상위는 `cand_arrive_min` 0.0531 · `is_tier3` 0.0320 · `max_capa` 0.0298 ·
`hingerate_capa` 0.0153 — **전부 카드가 쓰는 물리량**이다.

**원거리성 통제(부분상관)** — v19 교훈 재확인. `has_helipad` 는 통제 전 −0.1388 인데 통제 후
**+0.0141 로 부호가 바뀐다**(shrink 0.101). "헬기장 병원이 선택된다" 는 겉보기 상관이 거의 전부
거리 탓이다. 부하 신호(`load` 0.305 · `cand_p_sent` 0.316)는 통제 후에도 30% 남고,
시간 신호(`cand_arrive_min` 0.460 · `uav_advantage_min` 0.510)는 절반 남는다.

**모델 클래스가 중요도 순위를 바꾼다.** 트리 gain 상위는 `eta_raw_min`·`eta_rank`·`n_valid_actions`·
`amb_mean_return` 이고 `eta_rank` 는 로짓에서 음의 LOO 기여인데 트리에서는 2위(perm Δ재현율 +0.248).
→ **여러 방법이 일치하는 변수만 신뢰**한다. 도달시간과 부하항은 둘 다에서 상위(카드에 있다),
`eta_rank` 는 갈린다(카드에 없다 — 그게 맞았다).

**정보수준 절단 — 결정로그에서도 "내 발송" 이 "병원 재고" 보다 비싸다.**
NLL 비용(I3 대비): `NO_CENSUS` +0.054±0.012(14/83/153) · **`NO_OWN` +0.108±0.014(9/49/192)** ·
`NO_BOTH` +0.427±0.034(1/32/217). **내 발송 제거가 병원 재고 제거의 2배**이고,
둘 다 빼면 0.427 ≫ 0.054+0.108 로 **강한 초가법성**(두 부하 신호가 서로 대체재).
v21 이 폐루프에서 본 같은 방향(병원 재고만 쓰면 +0.0929 붕괴)을 **결정로그라는 독립 데이터**가 재확인한다.

**트리 홀드아웃 재현율 0.5986(우연 0.0253).** ⚠️**이 값을 폐루프 성능의 근거로 쓰지 않는다** —
v20 에서 교사 재현율 순위(K 0.3146 > … > Q 0.1792)가 폐루프 순서와 정확히 반대였고 채택 카드가
재현율 최하위였다. 링크 2 의 역할은 **변수 집합 선택**이고 숫자는 폐루프가 정한다.

**⑰ 등급 구성 축 — λ 공식은 구성으로도 확장되고, y 는 여기서도 공식이 안 된다 (신규 노브 + 36만 ep).**
`MCI_GRADE_COUNT="R,Y,G,B"` 노브 신설(`src/sim_src/{ScenarioManager,EventManager}.py`, 커밋 `ee9e14379`).
비율이 아니라 **인원수**를 받는다 — 지휘관은 트리아지를 끝내고 인원을 세기 때문이다.
다항분포 draw 는 **수행하고 결과만 버린다**(draw-and-discard): 건너뛰면 `self.rng` 스트림이 어긋나
이후 난수 전부가 달라진다. 실현값이 우연히 고정값과 같아지는 시드(686·1800)에서 **폐루프 전체 비트동일**로
의도대로 작동함을 확인했다. ⚠️완전 정렬은 **multinomial 까지만** 보장된다 — 구조시각 `beta(size=p_num[c])`
는 **인원수 자체가 난수 소비량**이라 구성이 실제로 달라지면 그 뒤는 원리상 갈린다.
미설정 비트동일 게이트: 3좌표 240값 + 30좌표 2,400값 + 정본 앵커(`lam_base.csv`) 2,400값, **전부 불일치 0**.

이 축은 물리 7축 스윕에서 유일하게 못 흔든 축이었다(비율이 CSV 에 고정). 총원 100·G50·B10 을 고정하고
이송 대상 `R+Y=40` 을 유지한 채 **Red 몫만** 0.10/0.25/0.50/0.75 로 움직였다(R=10,Y=30 이 명목 = 통제 셀).

**P1 (사전등록: λ\* 가 Red 몫과 단조 증가, 예측 17.5/18.7/20.6/22.5) — 확증.**

| Red 몫 | 0.10 | 0.25(명목) | 0.50 | 0.75 |
|---|---:|---:|---:|---:|
| 측정 λ\* | 18.74 | **19.13** | 21.58 | 26.13 |
| 예측 λ\* | 17.5 | 18.7 | 20.6 | 22.5 |

단조 증가 ✓ · 평균 절대오차 1.57분(7.6%) · 통제 셀 19.13 이 정본 18.97 과 일치.
**λ 공식이 서비스시간 *스케일* 뿐 아니라 *구성* 으로도 확장된다.**
⚠️기울기 `dlogλ/dlog(가중S) = +1.333` 으로 이론 +1 을 넘고 고 Red 몫에서 예측이 하향 편향된다 —
단순 가중평균이 **Red 가 tier3 를 독점하며 생기는 경합 심화**를 반영하지 못하는 탓으로 보인다(미검증 추론).

**P2 (사전등록: 예측 없음 — 열린 질문) — y\* 는 단조가 아니다.**

| Red 몫 | 0.10 | 0.25 | 0.50 | 0.75 |
|---|---:|---:|---:|---:|
| y\* | 2 | 4 | **8** | 4 |
| y\* 이득(vs y=0) | +0.000025±0.000087 (동률) | +0.000310±0.000178 (유의) | **+0.001978±0.000453** | **+0.001880±0.000341** |

**등급 축에 가장 직접적인 입력(트리아지 인원)을 넣어도 최적 임계가 단조로 움직이지 않는다.**
→ "y 는 공식이 없다" 는 결론이 가장 직접적인 축에서 확증됐다.
★단 **y 의 값어치는 Red 몫과 함께 커진다** — 명목에서 +0.00031 이던 이득이 Red 몫 0.50~0.75 에서
**+0.0019~+0.0020(판정선의 3.6~3.7배)** 가 된다. **폐루프가 가장 필요한 지점이 바로 여기다.**
비단조의 기전 후보(미검증): y 는 *대기 중인 Yellow 수* 에 대한 임계인데 Red 몫 0.75 면 Yellow 총원이
10명뿐이라 `yellow_wait ≤ 8` 이 거의 항상 참이 되어 임계가 포화한다. 절대수가 아니라 Yellow 총원 대비
상대값으로 쓰는 형태가 다음 가설이다.

⚠️`RuleManager.expected_R/expected_Y`(`RuleManager.py:118-119`)는 여전히 `ratio × incident_size` 라
이 노브를 켜면 명목과 어긋난다. START 트리아지 τ 계산에만 쓰이므로 `heur:`/`all_heur64` 경로만
해당하고 CARD(`cardt:`)·`cap3:`·PPO 는 영향이 없다(코드 경로 확인). 이번 스윕은 전부 안전한 팔만 썼다.

**미확정**: `RuleManager.expected_R` 의 노브 정합(등급 분포를 런타임에 못 바꾼다 — 현장 파이프라인이 명확한 에러를 낸다),
거리표 해상도 민감도(E9, 선택), 링크 2 변수 중요도(E6, 실행 중),
현행 교사 결정로그에 대한 변수 중요도 재적합, `test750` 최종 판정(설계 동결 후 1회),
`MCI_GRADE_RATIO` 노브 부재(등급 분포를 런타임에 못 바꾼다), 치료시간 4배 가정의 외부 타당성.

## 판정 전에 확인할 것

1. 가설·정보수준·학습/튜닝/판정 좌표·시드·예산·기준선·선택 규칙을 결과 보기 전에 정한다.
2. 모델/vecnorm/코드/시나리오·manifest hash/정책 스펙/모든 물리 노브를 맞춘다.
   물리조건이 바뀐 교사 zero-shot과 조건별 재학습을 구분한다.
3. CSV의 `(region, policy, seed)` 유일성, 예상 좌표·시드 집합의 완전성, 유한값,
   메타의 성공/행 수/hash를 검사한다. `.meta.json`·`DONE` 존재만으로 완주를 판단하지 않는다.
4. 두 팔을 **동일 지역·시드 키로 명시 정렬**해 직접 paired 비교한다. `cube()`는 내부 중복을
   평균내고 NumPy 배열에서는 키를 잃으므로 호출 전 중복·키 집합을 확인한다.
5. Δ=`PDR_baseline−PDR_candidate`(양수=개선). W/T/L은 지역별 에피소드 차이 95%CI로 센다.
   지역평균의 부호나 임의 epsilon으로 세지 않는다.
6. 규칙 대 학습정책의 현행 판정은 30시드 이상과 지역축·시드축 CI 및 시드 부호 일관성을 병기한다.
   학습 seed와 시뮬 seed는 다른 축이다. 구조 비교에는 복수 학습 seed(기존 기준 3개)가 필요하다.
7. 0.00053(당시 CRN 정밀도/실용선), 0.00114(과거 학습 seed sd)는 조건부 실측이다.
   새 실험의 보편 상수로 복사하지 말고 실제 paired CI를 구한다. 여러 후보 비교는 선택·다중검정을 명시한다.
8. 진행 중 CSV나 훈련곡선으로 채택하지 않는다. 완료 순서는 난이도에 의존한다.

## 남은 질문의 범위

주된 검증 대상은 규칙의 함수형·서비스시간 할인 기전·물리조건 전이·정보가용성이다.
v19 교사 학습 seed 반복과 시뮬 외부 타당성도 남아 있다. UAV 속도 가정은 실증 범위가 제한적이며,
Yellow 우선 결과는 이 시뮬의 총 예방가능 사망 목적함수에 조건부다.
“기존 문헌에 병원선택 RL이 없다”는 신규성 주장은 반례로 정정된 상태다.
논문 주장을 바꿀 때는 현행 실험 증거와 관련 원문을 다시 확인한다.
