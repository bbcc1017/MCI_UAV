# AGENTS.md

MCI_UAV의 에이전트 지침 정본. 확인: **2026-09-11 (v22 진행 중)**.
이 파일은 매 작업의 출발점이며, 상세 연구 이력은 여기에 누적하지 않는다.

## 연구 목적과 현재 방향

한국 대량사상자 사고(MCI)에서 구조된 환자의 **등급·목적지 병원·AMB/UAV 수단**을 결정해
예방가능 사망률 `PDR_woG`를 낮춘다. 전국 단일 RL에서 출발했으며 현재 메인트랙은
**PPO 교사 → 결정 로그 → 물리단위 현장 규칙 → 독립 좌표 폐루프 검증 → 기전·전이성 설명**이다.

분할은 `train6000 / budget750 / test750`, 교사는 field 관측 PPO, 규칙은 시간+대기행렬형
CARD다. v21 기준 `CARD_Q18`은 최강 교사와 **실용적 동률**이고 현장 누적발송을 쓰는 `CARD_P18`이
정보 요구를 줄인 카드다. 단순히 RL 점수나 모방 정확도를 높이는 것으로 연구 목표를 대체하지 않는다.
과거 챔피언·기각은 그 버전의 조건부 기록이다.

**현재 트랙은 v22 현장 어시스트**다 — 현장 지휘관의 상황 입력에서 CARD 파라미터를 정하는 모델과
그 실행시간 타당성. 2026-09-11 에 `budget750` 36조건 재도출로 확정된 것:
`λ`는 자유 상수가 아니라 **평균 서비스시간의 일차 함수**(치료시간 축 기울기 +1.121, 이론 +1)이고
공식이 시뮬 최적과 같은 배포값을 준다. 공식이 없는 축은 **등급 임계**뿐이며 폐루프의 역할이 거기 남는다.
제약은 계산이 아니라 **온라인 선택의 통계**다. 수치·조건·미확정 목록은 `agent_docs/research.md`가 정본이다.

## 작업 방식

- 한국어로 결론·근거·다음 행동을 간결하게 설명한다. 코드 주석·커밋 메시지도 한국어로,
  식별자/명령/Co-Authored-By는 원 표기를 유지한다. 사용자가 원하지 않은 압축 말투는 적용하지 않는다.
- 요청된 작업은 조사에서 멈추지 말고 구현·적절한 검증·결과 보고까지 마친다.
  되돌릴 수 있는 구현 선택은 합리적으로 결정한다. 기존에 받은 승인을 반복해서 묻지 않는다.
  연구 질문·데이터 역할·비가역 작업처럼 답이 결과를 바꾸는 사항만 필요한 시점에 확인한다.
- 세션의 상위 지침과 사용자의 현재 명시 요청이 이 파일·참조 문서·스킬보다 우선한다.
  오래된 “금지/진행 중/다음 실험”을 현재 요청으로 취급하지 않는다.
  스킬은 실제 필요할 때만 읽고, 지침 해석 때문에 멈춰야 하면 정확한 근거를 밝힌다.
- 먼저 cwd·git status·대상 범위를 확인하고, 해당 코드·직접 호출자·관련 근거부터 읽는다.
  `rg`/`rg --files`/`git ls-files`로 좁혀 찾는다. 코드 검색에서 results·archive·대형 GIS를
  기본 순회하지 않으며, 로컬 산출물은 명시 경로에서 별도 검색한다.
- 독립적인 읽기/검색은 도구가 지원하면 묶고, 편집·의존 단계·공유 출력 쓰기는 순서대로 한다.
  서브에이전트는 사용자 요청 또는 적용 지침이 명시적으로 허용할 때만 사용하며,
  파일 소유권·완료 기준을 나눈다. 루트가 통합과 장기 잡 완료를 책임진다.
- 검증 범위는 변경 위험에 맞춘다. 필요한 검증을 통과하면 근거 없이 넓히거나 반복하지 않는다.
  문서 수정에 학습을 돌리지 않고, 동작 변경을 문법 검사만으로 검증했다고 하지 않는다.
- 진행 중 새 요청은 기존 목표에 반영한다. 상태 질문에 답한 뒤 작업을 이어간다.
  장기 작업의 재개 정보는 해당 run에 남기고, PID·현재 자원·임시 할 일을 이 파일에 쌓지 않는다.

## 작업별로 읽을 문서

작업 범위에 해당하는 참조를 **작업 전** 읽는다. 모두를 매번 읽을 필요는 없다.
링크는 자동으로 로드됐다고 가정하지 않는다.

| 작업 | 우선 참조 |
|---|---|
| 실험 설계·평가·수치 인용·논문 주장 | [연구 상태·판정 계약](agent_docs/research.md); 필요 시 [발전사](RESEARCH_HISTORY.md)의 해당 버전/기각 |
| RL·관측·마스크·정책·시뮬 수정 | [코드 지도·계약](agent_docs/codebase.md) |
| 실행·학습·성능·장기 잡·복구·Git 정리 | [실행·검증·복구](agent_docs/operations.md) |
| 원본/가속 코어 작업 | 위 코드 계약 + [가속 경로 README](src/sim_src_upgrade/README.md)의 해당 절 |
| Unity·CAR_test·GIS | [CLAUDE.unity.md](CLAUDE.unity.md) + 존재하는 `.claude.local.md` 직접 읽기 |
| 지침/메모리 관리 | 이 파일 마지막 절 + [2026-09-09 개편 근거](agent_docs/2026-09-09-review.md) |

## 저장소·환경 경계

- `src/sim_src/`: 이산사건 시뮬 정본. `src/sim_src_upgrade/`: 별도 동치 가속 경로·검증기.
- `src/rl_src/`: flat import 기반 RL/규칙/평가. `v17_*`가 v21에서도 현행 진입점이다.
  `src/sce_src/`: 시나리오/분할. `tools/`: 라우팅·실험 driver·집계. `scoreboard/`: 버전별 프로토콜.
- Linux 기본 cwd=`/home/ryu/MCI_UAV`, Python=`/home/ryu/anaconda3/envs/UAV/bin/python`.
  공유 학습 노드이므로 worker 수는 **현재 부하·메모리·동시 작업**으로 정한다.
- **★GPU 는 device 0 만 쓴다 (2026-09-14 확정).** 이 노드에 GPU 가 2장 있다 —
  `0` = RTX A6000 · `1` = RTX 6000 Ada Generation(각 49 GiB, 드라이버 595.71.05).
  **device 1 은 타 사용자 몫이므로 우리 작업이 올라가지 않게 한다.**
  학습·추론·벤치 실행 전에 `CUDA_VISIBLE_DEVICES=0` 을 붙이고, 코드에서는 `cuda:0` 대신
  그 환경변수 뒤의 기본 장치를 쓴다(`CUDA_VISIBLE_DEVICES=0` 이면 프로세스 안에서 `cuda:0`
  이 물리 0번이다). `nvidia-smi` 로 점유를 확인하되 **device 1 의 부하는 우리 용량이 아니다.**
  `device_count()`·`cuda:1`·`torch.cuda.set_device(1)` 을 새로 넣지 않는다.
- `external/ml-agents`는 upstream submodule. 안의 `UAV_test/`·`CAR_test/`는 미추적 Windows
  로컬 자산이다. Unity/GIS 작업을 parent repo 커밋이나 submodule 포인터 변경에 섞지 않는다.
- `results/`, 생성 시나리오, 대부분 `docs/`, `archive/`는 gitignore다.
  `docs/assets/` 등 예외는 실제 추적 목록으로 확인한다. 로컬 문서가 없으면 코드·추적 문서·복원 원장을 찾는다.
  공개용 `agent_docs/`에 비공개 보고서·참고논문 원문·특허 문서를 복사하지 않는다.
- 기존 사용자 수정을 보존한다. 파일을 명시해 stage하며 `git add -A`·force-push는 사용하지 않는다.
  모델·래퍼·레거시는 import가 적어도 저장 모델/연구 재현에 쓰일 수 있으므로 도달성만으로 삭제하지 않는다.

## 항상 지킬 연구·구현 계약

1. **판정셋으로 튜닝하지 않는다.** 현행 선택은 budget750, 판정은 test750.
   `v19/tradeoff250_manifest.json`도 test750의 부분집합이다. 구 버전 seed/분할을 현행 기본값으로 복사하지 않는다.
2. **같은 조건의 완주 데이터로 직접 paired 비교한다.** 지역·정책·seed 키/중복/누락/hash를 확인한다.
   진행 중 CSV·훈련곡선·행동일치도로 채택하지 않는다. 학습 seed와 시뮬 seed의 불확실성을 구분한다.
3. **기준선·정보·물리조건을 숨기지 않는다.** 여러 교사 중 약한 것만 고르지 않는다.
   P18의 통신 불요는 부하 입력 수준이며 공통 안전마스크 정보까지 무통신으로 입증한 뜻은 아니다.
4. **shape·codec·정규화는 모델과 일치시킨다.** v19 교사는 `field`389차원이고
   `field+valid`436과 다르다. H47/action192를 모든 변형에 하드코딩하지 않는다.
   병원 블록을 슬롯별 정규화하지 않으며 모델과 같은 시점의 vecnorm/면제열을 사용한다.
5. **hard mask를 공통 제약으로 유지한다.** Red→tier3, UAV→helipad, 용량·가용성은 기존 mask/codec을 사용한다.
   규칙·RL·플래너가 각각 다른 적격성을 근사하지 않는다.
6. **보상 시각은 치료개시다.** `p_admit`은 병원 도착과 다르며 수술실 대기가 포함된다.
   발송상한·병원 수용량·수술실 서버수를 구분한다.
7. **격자 끝이 최적이면 그것은 최적이 아니다.** 응답면의 최소점이 스윕 격자의 양끝이면
   진짜 최적은 격자 밖이고, 그 점을 회귀·회수율·전이후회에 넣으면 조용히 편향된다.
   격자를 넓혀 내부 최소점을 확보한 뒤 판정한다. 실측 사례 둘 — v20 치료시간 기울기가
   +1.06으로 낮게 나온 것과, 봉투 밖 재튜닝 회수율이 94.9%로 과소평가된 것(참값 100%).
8. **불변성·전이성 주장의 조건집합에 그 상수가 의존하는 축을 반드시 포함한다.**
   v20은 "λ는 서비스시간 그 자체"와 "전이후회 평균 +0.00010"을 함께 주장했지만 두 계산의
   조건집합이 서로소였다(`transfer.csv` 27조건에 치료시간 축이 0개). 그 축을 넣으면 최대 후회가
   +0.00057에서 +0.02775로 커진다. 축 목록을 먼저 적고 빠진 축을 명시한다.
9. **시뮬 정본·과거 결과를 보존한다.** 변형은 wrapper/opt-in을 우선한다.
   요청된 코어 수정은 원본과 가속 사본·해당 등가성을 함께 확인한다.
   게이트 실패를 threshold 완화·step 증가·hash 갱신만으로 우회하지 않는다.

## 메모리 유지 원칙

- **AGENTS.md가 단일 지침 정본**, `CLAUDE.md`는 이 파일을 읽는 짧은 진입점이다.
  `cp CLAUDE.md AGENTS.md` 미러 동기화는 폐기한다.
- 루트는 **UTF-8 10KiB 이내를 목표**로 목적·라우팅·반복 실패 방지 계약만 유지한다.
  상세 코드/실행 계약은 `agent_docs/`, 버전별 결과는 `RESEARCH_HISTORY.md`,
  원시 수치·개별 실험 메타는 `results/<run>/`에 기록한다.
- 새 결론은 날짜·조건·근거 경로·대체된 결론을 함께 적는다. 기존 “현재/다음/기각”과 충돌하면
  그 문장도 수정한다. 추정은 추정으로, 검증 안 한 한계는 미검증으로 남긴다.
- 근거가 충돌하면 **실행 의미는 코드+실제 모델/메타**, **실험 결론은 해당 버전 프로토콜+
  완주 paired 산출물**, 이력은 발전사를 확인한다. 주석·README·날짜만으로 결론을 확정하지 않는다.
- 개편 전 전문은 `git show 0b749f07e:AGENTS.md`로 보존돼 있다.
  새 지침이 과거 작업 로그를 재수입하며 비대해지지 않게 한다.
