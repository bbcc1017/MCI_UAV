# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#   Usually these files are written by a python script from a template
#   before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
# Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
# uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
# poetry.lock
# poetry.toml

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
# pdm.lock
# pdm.toml
.pdm-python
.pdm-build/

# pixi
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
# pixi.lock
#   Pixi creates a virtual environment in the .pixi directory, just like venv module creates one
#   in the .venv directory. It is recommended not to include this directory in version control.
.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# Redis
*.rdb
*.aof
*.pid

# RabbitMQ
mnesia/
rabbitmq/
rabbitmq-data/

# ActiveMQ
activemq-data/

# SageMath parsed files
*.sage.py

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# Node 아티팩트(이 레포는 Python/C# — package.json 없음. 도구가 루트서 npm 건드릴 때
# 생기는 떠돌이 stub 방어)
package-lock.json
node_modules/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#   JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#   be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#   and can be added to the global gitignore or merged into this file.  For a more nuclear
#   option (not recommended) you can uncomment the following to ignore the entire idea folder.
# .idea/

# Abstra
#   Abstra is an AI-powered process automation framework.
#   Ignore directories containing user credentials, local state, and settings.
#   Learn more at https://abstra.io/docs
.abstra/

# Visual Studio Code
.vscode/
# Temporary file for partial code execution
tempCodeRunnerFile.py

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# Streamlit
.streamlit/secrets.toml

# Claude Code local settings
.claude/
.claude.local.md

# Unity
Library/

# Local OSM/OSRM runtime data
osrm_data/
overpass_db/
Temp/
Obj/
Build/
Builds/
Logs/
UserSettings/
MemoryCaptures/
*.csproj
*.sln
*.user
*.unityproj
*.tmp
*.suo
*.pidb
*.booproj
*.svd
*.pdb.meta
*.opendb
*.VC.db
sysinfo.txt
*.apk
*.aab
*.unitypackage

# ML-Agents training outputs
results/
results_archived*/
*.onnx.meta

# MCI_UAV generated artifacts
# - 자동 생성 시나리오 (사고지점별 폴더, 옛 평면 구조)
scenarios/exp_*/
# - 한국도로공사 고속도로 사고 원자료 (58MB, 외부 검증용 입력 — 재배포 대상 아님)
scenarios/highway_accident.csv
# - 평가점 시나리오 생성 진행상태 매니페스트 (생성 중 매 시나리오마다 갱신)
scenarios/plan1nat_eval_manifest.json
# - hold-out 평가용 flatten 매니페스트 (절대경로 config 참조 — 환경 의존, 스크립트로 재생성 가능)
scenarios/plan1nat_holdout_flat.json
scenarios/*_holdout_*.json
# - 재정리 후 실험별 상위 폴더 (helipad_center / korea_center 하위에 자동 생성된 시나리오)
scenarios/helipad_center/
scenarios/korea_center/
# - 임시 좌표 파일 (시나리오 생성 중간 산출)
scenarios/**/_hospital_coords.csv
# - 시뮬레이션 / 시나리오 로그
experiment_logs/
# - tmp 파이프 로그
/tmp/
*.tfevents.*
# Codex
.codex/
# 에이전트 스킬 설치 산출물(caveman 등 `npx skills add`) — 도구지 프로젝트 코드 아님
.agents/
skills-lock.json
# 문서
docs/

# 시나리오 생성 실패 로그(재실행 시 재생성 — 산출물 아님)
scenarios/manifests/*.failed.json

# Claude Code 로컬 전용 메모(Unity 작업 등) — 팀 공유 CLAUDE.md와 분리
.claude.local.md

# tools 대용량 데이터 (재생성 가능 — 스크립트만 버전관리)
tools/vw_*/
tools/nationwide/blocks/
tools/nationwide/sgg/
# OSM 시군구별 산출물 (osm_*.py 로 재생성 — 스크립트만 버전관리)
tools/nationwide/area/
tools/nationwide/.water2/
tools/nationwide/feat/
tools/nationwide/poi/
tools/nationwide/roads2/
tools/nationwide/roads/
tools/nationwide/infra/
tools/nationwide/footways/
# HD맵(정밀도로지도)·NGII 도로중심선·roads2 백업 — 대용량 재생성물(hdmap_fetch.py 등 스크립트만 버전관리)
tools/nationwide/hdmap/
tools/nationwide/ngiiroads/
tools/nationwide/roads2_old/
# 건물 높이 인덱스 (vworld_buildings_batch.py 로 재생성 — 255지역 492MB)
tools/nationwide/bldgindex/
# v2 디지털트윈 산출물 (정사/DEM/타일 버킷 — 전부 재생성 가능; qa 리포트만 추적)
tools/nationwide_v2/*
!tools/nationwide_v2/qa/
# 참고 논문 원문 (저작물 — 버전관리 제외)
/scratch_paper.txt
# 지형 DEM(.bin/.json)·정사 모자이크 — 대용량 재생성물(terrain_fetch.py / vworld_fetch.py)
tools/nationwide/terrain/
tools/nationwide/ortho_fill/
tools/nationwide/layer_audit.txt
tools/nationwide/_poifeat_start
tools/nationwide/*.json
tools/nationwide/*.log
tools/nationwide/*.err.log
tools/nationwide/*.output
tools/seoul_pilot/_work/
tools/_tmp_*.yaml
tools/**/_hospital_coords.csv
osrm_data/

# ─────────────────────────────────────────────────────────────────────────────
# 2026-09-06 레포 정돈
# ─────────────────────────────────────────────────────────────────────────────
# 종결 자산 보관소 — 원장(README)만 추적
archive/*
!archive/README.md

# Windows 박스에서 콜드데이터를 Y:(=이 레포 루트) 로 옮기다 중단된 잔해.
# 출처 미확인 상태로 보류 중 — 정체가 확인되면 레포 밖으로 옮기거나 지울 것.
MCI_UAV_cold/

# Unity 디지털트윈 / CAR_test 전용 GIS 도구 — 로컬-C 전용이라 추적하지 않는다.
# 설명·복원절차는 CLAUDE.unity.md 머리말. 워킹트리만 되살리려면
#   git restore --source=9f4d4fc --worktree -- tools/
# `git checkout 9f4d4fc -- tools/` 는 인덱스까지 갱신해 재추적되니 쓰지 말 것.
# ⚠️ RL/sim 공용인 osrm_*.{ps1,sh} 와 build_distance_matrix_osrm.py 는 추적 유지.
tools/airspace_fetch.py
tools/bldg_height_index.py
tools/build_region_index.py
tools/hdmap_fetch.py
tools/nationwide_build.py
tools/nationwide/monitor.py
tools/osm_areas.py
tools/osm_explore.py
tools/osm_features.py
tools/osm_infra.py
tools/osm_overpass_endpoints.py
tools/osm_poi.py
tools/osm_roads.py
tools/osm_roads2.py
tools/osm_water.py
tools/osm_fetch_local.ps1
tools/run_sim_trace.py
tools/scene_export.py
tools/trace_export.py
tools/seoul_pilot/build_scene_data.py
tools/shp_multi_to_buildings.py
tools/terrain_fetch.py
tools/terrain_smooth.py
tools/v2_*.py
tools/vworld_buildings_batch.py
tools/vworld_fetch.py
