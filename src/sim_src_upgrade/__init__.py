"""sim_src 고속 사본 패키지 — 결과 동일, 연산시간만 단축.

`src/sim_src/` 는 정본이며 **절대 수정하지 않는다**(실행 중인 전수평가 드라이버가
소스 sha256 을 워커마다 검증한다). 이 패키지는 같은 로직의 별도 실행경로다.

핵심 설계
---------
* `core/` 는 `sim_src` 의 사본이되 **패키지 내부 상대 import** 로 바꿨다.
  덕분에 구 코어(flat `EventManager`)와 신 코어(`sim_src_upgrade.core.EventManager`)를
  **한 프로세스에 동시에** 올릴 수 있고, 그게 동치검증(G1/G2)의 전제다.
* 최적화는 "로직 동일" 만 허용한다. RNG 드로우 수·순서, 부동소수 연산 순서,
  정렬 tie-break, `list.pop()` 순서는 불변이어야 한다.
* 사본이므로 원본이 바뀌면 드리프트가 생긴다 → `origin_sync.py --check`(G0)가 감시한다.

import 부작용을 두지 않는다(sys.path 조작·환경변수 설정 없음).
"""

__all__ = ["core"]
